bin/
*.exe
*.exe~
coverage.out
coverage.txt
# Windows resource objects, regenerated by scripts/gen-winres.sh on every build
# so the embedded version always matches the binary's ldflags stamp.
cmd/gaia/*.syso
