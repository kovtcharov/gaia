{
  "components": {
    "schemas": {
      "ActionItem": {
        "additionalProperties": false,
        "description": "A single extracted action the principal should take.",
        "properties": {
          "description": {
            "description": "Imperative action, e.g. 'Reply to Bob'.",
            "title": "Description",
            "type": "string"
          },
          "due_hint": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Free-text due hint as written ('Friday', 'EOD'); not parsed.",
            "title": "Due Hint"
          },
          "type": {
            "default": "text",
            "description": "Discriminator: 'text' for a plain imperative action; 'link' when the action involves following a URL (url is then required and non-empty).",
            "enum": [
              "text",
              "link"
            ],
            "title": "Type",
            "type": "string"
          },
          "url": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The URL to follow for a 'link' action item; None for 'text'.",
            "title": "Url"
          }
        },
        "required": [
          "description"
        ],
        "title": "ActionItem",
        "type": "object"
      },
      "AttachmentMeta": {
        "additionalProperties": false,
        "description": "Metadata for one attachment on a message (schema 2.2, #1542).\n\nMetadata only — no content. On the read path ``attachment_id`` is the\nprovider handle a caller uses to fetch the bytes (Gmail\n``body.attachmentId``); it is null for providers/parts that carry none.",
        "properties": {
          "attachment_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Provider handle for fetching the content (Gmail body.attachmentId). Null when the provider/part carries none.",
            "title": "Attachment Id"
          },
          "filename": {
            "description": "Attachment filename, e.g. 'report.pdf'.",
            "title": "Filename",
            "type": "string"
          },
          "mime_type": {
            "description": "MIME type as reported by the provider.",
            "title": "Mime Type",
            "type": "string"
          },
          "size_bytes": {
            "description": "Attachment size in bytes (decoded).",
            "minimum": 0.0,
            "title": "Size Bytes",
            "type": "integer"
          }
        },
        "required": [
          "filename",
          "mime_type",
          "size_bytes"
        ],
        "title": "AttachmentMeta",
        "type": "object"
      },
      "AttentionCoverage": {
        "additionalProperties": false,
        "description": "How much of the mailbox this attention view actually covered.\n\nCarries the same honesty fields #2584 established for the pre-scan\nenvelope, so the renderer can state what was scanned rather than let\n'nothing needs you' read as an unqualified whole-mailbox claim.",
        "properties": {
          "degraded": {
            "default": false,
            "description": "True when at least one connected mailbox could not be scanned (see mailbox_errors), OR at least one individual message could not be fetched after Gmail rate-limited it past its retry budget (see message_errors, #2716) — either way, the surviving results are still shown, but coverage is partial.",
            "title": "Degraded",
            "type": "boolean"
          },
          "mailbox_errors": {
            "anyOf": [
              {
                "items": {
                  "$ref": "#/components/schemas/MailboxError"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "description": "Connected mailboxes that failed during this scan, if any.",
            "title": "Mailbox Errors"
          },
          "message_errors": {
            "anyOf": [
              {
                "items": {
                  "$ref": "#/components/schemas/MessageError"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "description": "Individual messages that could not be fetched during this scan, if any (#2716) — e.g. a Gmail rate-limit that survived retry. Every other message in the same mailbox is still included in ``items``; this is a per-message gap, not a mailbox failure.",
            "title": "Message Errors"
          },
          "scan_truncated": {
            "default": false,
            "description": "True when the scan hit its message ceiling in any connected mailbox — older mail may exist beyond what was looked at.",
            "title": "Scan Truncated",
            "type": "boolean"
          },
          "scanned": {
            "default": 0,
            "description": "Messages actually scanned across every mailbox.",
            "title": "Scanned",
            "type": "integer"
          },
          "total_unread": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Exact total unread count when the backend can report it honestly (Gmail); null when it can't (Outlook) — never a fabricated number.",
            "title": "Total Unread"
          }
        },
        "title": "AttentionCoverage",
        "type": "object"
      },
      "AttentionItem": {
        "additionalProperties": false,
        "description": "One item the attention view surfaces, tagged with why it's here.\n\nPassive data only — the view never acts on a message, so this carries no\naction affordance, just enough to identify the message and explain the\nreason a person should look at it.",
        "properties": {
          "due_hint": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Free-text due hint (action items only); null otherwise.",
            "title": "Due Hint"
          },
          "kind": {
            "$ref": "#/components/schemas/AttentionItemKind",
            "description": "Which signal surfaced this item."
          },
          "mailbox": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Provider name ('google' / 'microsoft' / 'microsoft_work') this item came from. Set only when more than one mailbox is connected — with a single mailbox, tagging every row is noise, not information.",
            "title": "Mailbox"
          },
          "message_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Provider message id (opaque). Null for an action_item with no recoverable source message (e.g. a pre-#1605 task row).",
            "title": "Message Id"
          },
          "sender": {
            "default": "",
            "description": "Raw 'From' header of the source message.",
            "title": "Sender",
            "type": "string"
          },
          "subject": {
            "default": "",
            "description": "Subject line of the source message. For an action_item this is the extracted action description, not an email subject.",
            "title": "Subject",
            "type": "string"
          },
          "thread_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Provider thread id, when known.",
            "title": "Thread Id"
          },
          "why": {
            "description": "Plain-language reason this item needs attention.",
            "title": "Why",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "why"
        ],
        "title": "AttentionItem",
        "type": "object"
      },
      "AttentionItemKind": {
        "description": "Why one attention-view item is here — the source signal it came from.\n\nDefined here (ahead of the ATTENTION VIEW section below, #2743) so\n``NeedsYouItem.kind`` — which reuses this enum rather than a parallel\nverb enum — resolves eagerly instead of as a class-body-time forward\nreference; see the ATTENTION VIEW section's comment for why that matters.\n\n``URGENT`` / ``NEEDS_RESPONSE`` were added after the initial #2743 cut:\n``needs_you`` originally tagged every category-classified item\n``WAITING_ON_YOU``, which collided with that value's real, published\nmeaning (the waiting-on-you *detector* found a thread awaiting reply) the\nmoment the detector's own output was wired into the same view. ``kind``\nis pure provenance — which signal put this row here — never a verb; the\nREPLY/DECIDE/CHECK label a renderer shows is a render-time lookup.",
        "enum": [
          "meeting_request",
          "waiting_on_you",
          "needs_review",
          "action_item",
          "urgent",
          "needs_response"
        ],
        "title": "AttentionItemKind",
        "type": "string"
      },
      "BatchItemError": {
        "additionalProperties": false,
        "description": "Why one batch item could not be triaged (surfaced loudly, never swallowed).\n\nKept as an object (not a bare string) so a future ``code``/``type`` field is\nadditive rather than another breaking change.",
        "properties": {
          "message": {
            "description": "Actionable failure reason for this item.",
            "title": "Message",
            "type": "string"
          }
        },
        "required": [
          "message"
        ],
        "title": "BatchItemError",
        "type": "object"
      },
      "BatchItemResult": {
        "additionalProperties": false,
        "description": "One item's outcome in a batch triage, correlated by 0-based index.\n\nExactly one of ``result`` or ``error`` is set. ``index`` matches the item's\nposition in the request ``items`` array so out-of-order processing is safe\n(the current implementation is sequential and order-preserving).",
        "properties": {
          "error": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/BatchItemError"
              },
              {
                "type": "null"
              }
            ],
            "description": "Set when the item failed."
          },
          "index": {
            "description": "0-based position in the request ``items`` array.",
            "minimum": 0.0,
            "title": "Index",
            "type": "integer"
          },
          "result": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/EmailTriageResult"
              },
              {
                "type": "null"
              }
            ],
            "description": "Set when the item succeeded."
          }
        },
        "required": [
          "index"
        ],
        "title": "BatchItemResult",
        "type": "object"
      },
      "BatchTriageRequest": {
        "additionalProperties": false,
        "description": "Top-level batch triage request envelope (#1887).\n\nAccepts 1..MAX_BATCH_SIZE ``EmailInput`` items (same discriminated union as\nthe single-email endpoint's ``payload``). ``context`` applies to ALL items;\nper-item context override is out of scope.",
        "properties": {
          "context": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TriageContext"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional context (people/projects/tone/self-email) applied to ALL items. Absent → behavior unchanged for every item."
          },
          "items": {
            "description": "The emails / threads to triage, 1..MAX_BATCH_SIZE items. Each is a SingleEmailInput or ThreadInput discriminated by ``kind``.",
            "items": {
              "discriminator": {
                "mapping": {
                  "single": "#/components/schemas/SingleEmailInput",
                  "thread": "#/components/schemas/ThreadInput"
                },
                "propertyName": "kind"
              },
              "oneOf": [
                {
                  "$ref": "#/components/schemas/SingleEmailInput"
                },
                {
                  "$ref": "#/components/schemas/ThreadInput"
                }
              ]
            },
            "maxItems": 100,
            "minItems": 1,
            "title": "Items",
            "type": "array"
          },
          "schema_version": {
            "default": "2.14",
            "description": "Contract version. Mismatch lets a consumer fail loudly.",
            "title": "Schema Version",
            "type": "string"
          }
        },
        "required": [
          "items"
        ],
        "title": "BatchTriageRequest",
        "type": "object"
      },
      "BatchTriageResponse": {
        "additionalProperties": false,
        "description": "Top-level batch triage response envelope (#1887).\n\nOne ``BatchItemResult`` per request item, order-preserved, 1:1 with\n``items``. HTTP 200 with all items errored is valid — consumers MUST inspect\neach ``results[].error``, not just the HTTP status.",
        "properties": {
          "results": {
            "description": "One entry per request item, order-preserved, 1:1 with ``items``.",
            "items": {
              "$ref": "#/components/schemas/BatchItemResult"
            },
            "title": "Results",
            "type": "array"
          },
          "schema_version": {
            "default": "2.14",
            "description": "Echoes the contract version.",
            "title": "Schema Version",
            "type": "string"
          }
        },
        "required": [
          "results"
        ],
        "title": "BatchTriageResponse",
        "type": "object"
      },
      "BulkSummary": {
        "additionalProperties": false,
        "description": "The filtered remainder (#2743) — a count PLUS the test(s) that\nfiltered it, so the user can judge whether the filter was right, never\njust a bare unauditable number.",
        "properties": {
          "count": {
            "default": 0,
            "description": "Messages filtered into the low-signal remainder.",
            "title": "Count",
            "type": "integer"
          },
          "filter_tests": {
            "description": "Ids (never prose) of the filter tests actually applied this run — a renderer maps each id to a sentence. An unmapped id degrades visibly rather than rendering a stale claim.",
            "items": {
              "type": "string"
            },
            "title": "Filter Tests",
            "type": "array"
          }
        },
        "title": "BulkSummary",
        "type": "object"
      },
      "CalendarCreateEventRequest": {
        "additionalProperties": false,
        "description": "Create a calendar event. Shared by the preview (token-mint) and create\n(token-consume) endpoints.\n\nCreating an event is a Tier-2 (externally visible) mutation, so it is gated\nby the same single-use confirmation-token handshake as ``/v1/email/send``:\nPOST this to ``/calendar/events/preview`` to mint a token bound to the exact\nevent, then echo the token in ``confirmation_token`` to ``/calendar/events``.",
        "properties": {
          "attendees": {
            "description": "Attendee email addresses to invite (may be empty).",
            "items": {
              "type": "string"
            },
            "title": "Attendees",
            "type": "array"
          },
          "confirmation_token": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Confirmation token from POST /v1/email/calendar/events/preview. Ignored by preview; required by create — a create without a valid token bound to this exact event is rejected (403).",
            "title": "Confirmation Token"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional event description / body.",
            "title": "Description"
          },
          "end": {
            "$ref": "#/components/schemas/CalendarEventDateTime",
            "description": "Event end (after start)."
          },
          "location": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional free-text location.",
            "title": "Location"
          },
          "provider": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional provider binding ('google', 'microsoft', or 'microsoft_work'). When set on preview, the confirmation token binds to this provider so the create routes to the right calendar even with multiple accounts connected.",
            "title": "Provider"
          },
          "schema_version": {
            "default": "2.14",
            "description": "Contract version. Mismatch lets a consumer fail loudly.",
            "title": "Schema Version",
            "type": "string"
          },
          "start": {
            "$ref": "#/components/schemas/CalendarEventDateTime",
            "description": "Event start."
          },
          "summary": {
            "description": "Event title / summary (non-empty).",
            "title": "Summary",
            "type": "string"
          }
        },
        "required": [
          "summary",
          "start",
          "end"
        ],
        "title": "CalendarCreateEventRequest",
        "type": "object"
      },
      "CalendarEvent": {
        "additionalProperties": false,
        "description": "A calendar event as returned by the view endpoint (display-flattened).\n\n``start`` / ``end`` are the raw provider strings (an RFC 3339 ``dateTime``\nfor timed events or a ``YYYY-MM-DD`` ``date`` for all-day events) — flattened\nfor display rather than re-modeled, matching what the agent's calendar tools\nsurface.",
        "properties": {
          "end": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "End instant ('dateTime') or all-day 'date'.",
            "title": "End"
          },
          "id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Provider event id (opaque).",
            "title": "Id"
          },
          "location": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Free-text location, or null when none.",
            "title": "Location"
          },
          "organizer": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Organizer email, or null when not reported.",
            "title": "Organizer"
          },
          "start": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Start instant ('dateTime') or all-day 'date'.",
            "title": "Start"
          },
          "summary": {
            "default": "",
            "description": "Event title / summary.",
            "title": "Summary",
            "type": "string"
          }
        },
        "title": "CalendarEvent",
        "type": "object"
      },
      "CalendarEventDateTime": {
        "additionalProperties": false,
        "description": "One endpoint (start or end) of a calendar event.\n\nProvide EXACTLY ONE of:\n  - ``date_time`` — a timed event, RFC 3339 (e.g. ``2026-07-01T14:00:00Z``\n    or ``2026-07-01T14:00:00-07:00``); or\n  - ``date`` — an all-day event, ``YYYY-MM-DD``.\n\n``time_zone`` is an optional IANA name (e.g. ``America/Los_Angeles``).\nNote: when ``time_zone`` is omitted on a timed event, the Microsoft Graph\n(Outlook) calendar backend attaches a default of ``UTC`` (Graph rejects a\nbare ``dateTime`` without a paired time zone). For Google, supply either a\nUTC offset inside ``date_time`` or an explicit ``time_zone``.",
        "properties": {
          "date": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "All-day date, 'YYYY-MM-DD'. Mutually exclusive with 'date_time'.",
            "title": "Date"
          },
          "date_time": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Timed-event instant, RFC 3339. Mutually exclusive with 'date'.",
            "title": "Date Time"
          },
          "time_zone": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "IANA time zone (e.g. 'America/Los_Angeles'). Optional; the Outlook backend defaults a missing time zone to 'UTC' for timed events.",
            "title": "Time Zone"
          }
        },
        "title": "CalendarEventDateTime",
        "type": "object"
      },
      "CalendarEventPreviewResponse": {
        "additionalProperties": false,
        "description": "The normalized event echo plus a single-use confirmation token bound to\nit. Returned by POST /v1/email/calendar/events/preview.",
        "properties": {
          "attendees": {
            "description": "Attendees to invite.",
            "items": {
              "type": "string"
            },
            "title": "Attendees",
            "type": "array"
          },
          "confirmation_token": {
            "description": "Echo this back to POST /v1/email/calendar/events to authorize creating exactly this event. Single-use; bound to the event payload.",
            "title": "Confirmation Token",
            "type": "string"
          },
          "description": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional description.",
            "title": "Description"
          },
          "end": {
            "$ref": "#/components/schemas/CalendarEventDateTime",
            "description": "Event end."
          },
          "location": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional location.",
            "title": "Location"
          },
          "schema_version": {
            "default": "2.14",
            "description": "Echoes the contract version.",
            "title": "Schema Version",
            "type": "string"
          },
          "start": {
            "$ref": "#/components/schemas/CalendarEventDateTime",
            "description": "Event start."
          },
          "summary": {
            "description": "The event title to be created.",
            "title": "Summary",
            "type": "string"
          }
        },
        "required": [
          "summary",
          "start",
          "end",
          "confirmation_token"
        ],
        "title": "CalendarEventPreviewResponse",
        "type": "object"
      },
      "CalendarEventResponse": {
        "additionalProperties": false,
        "description": "Result of POST /v1/email/calendar/events (create — confirmation-gated).",
        "properties": {
          "created": {
            "default": true,
            "description": "Always true on success.",
            "title": "Created",
            "type": "boolean"
          },
          "event_id": {
            "description": "Provider id of the created event.",
            "title": "Event Id",
            "type": "string"
          },
          "schema_version": {
            "default": "2.14",
            "description": "Echoes the contract version.",
            "title": "Schema Version",
            "type": "string"
          },
          "summary": {
            "description": "Title of the created event.",
            "title": "Summary",
            "type": "string"
          }
        },
        "required": [
          "event_id",
          "summary"
        ],
        "title": "CalendarEventResponse",
        "type": "object"
      },
      "CalendarEventsResponse": {
        "additionalProperties": false,
        "description": "Result of GET /v1/email/calendar/events (read-only view).",
        "properties": {
          "events": {
            "description": "Matching events, ordered by start time.",
            "items": {
              "$ref": "#/components/schemas/CalendarEvent"
            },
            "title": "Events",
            "type": "array"
          },
          "schema_version": {
            "default": "2.14",
            "description": "Echoes the contract version.",
            "title": "Schema Version",
            "type": "string"
          }
        },
        "title": "CalendarEventsResponse",
        "type": "object"
      },
      "CalendarRespondRequest": {
        "additionalProperties": false,
        "description": "RSVP to an existing calendar invite (POST /v1/email/calendar/events/respond).\n\nResponding to an invite is an explicit, user-initiated action surfaced by the\nUI's accept/decline controls, so it is not separately token-gated. ``status``\nis the RSVP verb; ``attendee_email`` is the principal's own address (used by\nthe Google backend to locate the attendee row; ignored by the Outlook backend,\nwhich RSVPs on the authenticated ``/me`` calendar).",
        "properties": {
          "attendee_email": {
            "description": "The principal's own email (the attendee responding). Used by the Google backend; ignored by Outlook (RSVPs on /me).",
            "title": "Attendee Email",
            "type": "string"
          },
          "event_id": {
            "description": "Provider event id to RSVP to.",
            "title": "Event Id",
            "type": "string"
          },
          "provider": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional provider binding ('google', 'microsoft', or 'microsoft_work').",
            "title": "Provider"
          },
          "schema_version": {
            "default": "2.14",
            "description": "Contract version. Mismatch lets a consumer fail loudly.",
            "title": "Schema Version",
            "type": "string"
          },
          "status": {
            "description": "RSVP response: accept, decline, or tentatively accept.",
            "enum": [
              "accepted",
              "declined",
              "tentative"
            ],
            "title": "Status",
            "type": "string"
          }
        },
        "required": [
          "event_id",
          "status",
          "attendee_email"
        ],
        "title": "CalendarRespondRequest",
        "type": "object"
      },
      "CalendarRespondResponse": {
        "additionalProperties": false,
        "description": "Result of POST /v1/email/calendar/events/respond (RSVP).",
        "properties": {
          "event_id": {
            "description": "The event that was responded to.",
            "title": "Event Id",
            "type": "string"
          },
          "responded": {
            "default": true,
            "description": "Always true on success.",
            "title": "Responded",
            "type": "boolean"
          },
          "schema_version": {
            "default": "2.14",
            "description": "Echoes the contract version.",
            "title": "Schema Version",
            "type": "string"
          },
          "status": {
            "description": "The RSVP response that was recorded.",
            "enum": [
              "accepted",
              "declined",
              "tentative"
            ],
            "title": "Status",
            "type": "string"
          }
        },
        "required": [
          "event_id",
          "status"
        ],
        "title": "CalendarRespondResponse",
        "type": "object"
      },
      "DraftReply": {
        "additionalProperties": false,
        "description": "A drafted reply the agent proposes. Never sent without confirmation\n(#1264) — this is a proposal, not an action.",
        "properties": {
          "attachments": {
            "description": "Metadata of the attachments the draft proposes to send (schema 2.2, #1542). Content stays in the caller's OutgoingAttachment payload; only the echo travels here.",
            "items": {
              "$ref": "#/components/schemas/AttachmentMeta"
            },
            "title": "Attachments",
            "type": "array"
          },
          "body": {
            "description": "Proposed reply body.",
            "title": "Body",
            "type": "string"
          },
          "subject": {
            "description": "Proposed subject line.",
            "title": "Subject",
            "type": "string"
          },
          "to": {
            "description": "Proposed recipients (non-empty).",
            "items": {
              "$ref": "#/components/schemas/EmailAddress"
            },
            "minItems": 1,
            "title": "To",
            "type": "array"
          }
        },
        "required": [
          "to",
          "subject",
          "body"
        ],
        "title": "DraftReply",
        "type": "object"
      },
      "DraftScaffold": {
        "additionalProperties": false,
        "description": "A reply *scaffold* the triage path proposes — recipient + subject only,\nno body (schema 2.3).\n\nTriage classifies and summarizes; it never composes reply prose. So the\nscaffold it returns carries only the resolved reply recipient and a\n``Re:``-prefixed subject. To turn it into a sendable reply, compose the body\nyourself (e.g. an LLM call over the triage ``summary`` + ``action_items`` +\nthe original message) and pass ``(to, subject, body)`` to\n``POST /v1/email/draft`` — that endpoint returns a full :class:`DraftReply`\nand a single-use send-confirmation token.",
        "properties": {
          "subject": {
            "description": "Proposed subject line.",
            "title": "Subject",
            "type": "string"
          },
          "to": {
            "description": "Proposed recipients (non-empty).",
            "items": {
              "$ref": "#/components/schemas/EmailAddress"
            },
            "minItems": 1,
            "title": "To",
            "type": "array"
          }
        },
        "required": [
          "to",
          "subject"
        ],
        "title": "DraftScaffold",
        "type": "object"
      },
      "EmailActionConfirmRequest": {
        "additionalProperties": false,
        "description": "Request a single-use confirmation token for a destructive mailbox action.\n\nMirrors the draft→send handshake (#1264): nothing mutates here — this only\nmints a token bound to *this* ``(action, message_id)`` that the matching\n``/archive`` or ``/quarantine`` call must echo back. A token minted for one\naction/message cannot authorize a different one.",
        "properties": {
          "action": {
            "description": "The action to authorize: 'archive' or 'quarantine'.",
            "enum": [
              "archive",
              "quarantine"
            ],
            "title": "Action",
            "type": "string"
          },
          "message_id": {
            "description": "Provider message id the action will mutate.",
            "title": "Message Id",
            "type": "string"
          },
          "provider": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional provider binding ('google' / 'microsoft' / 'microsoft_work'). When set, the minted token is bound to this mailbox so the action routes correctly even when more than one mailbox is connected.",
            "title": "Provider"
          }
        },
        "required": [
          "action",
          "message_id"
        ],
        "title": "EmailActionConfirmRequest",
        "type": "object"
      },
      "EmailActionConfirmResponse": {
        "additionalProperties": false,
        "description": "A single-use confirmation token bound to the requested action+message.",
        "properties": {
          "action": {
            "description": "The authorized action.",
            "enum": [
              "archive",
              "quarantine"
            ],
            "title": "Action",
            "type": "string"
          },
          "confirmation_token": {
            "description": "Echo to POST /v1/email/archive or /v1/email/quarantine to authorize exactly this (action, message_id). Single-use; bound to the action.",
            "title": "Confirmation Token",
            "type": "string"
          },
          "message_id": {
            "description": "The message the token authorizes.",
            "title": "Message Id",
            "type": "string"
          },
          "schema_version": {
            "default": "2.14",
            "description": "Echoes the contract version.",
            "title": "Schema Version",
            "type": "string"
          }
        },
        "required": [
          "confirmation_token",
          "action",
          "message_id"
        ],
        "title": "EmailActionConfirmResponse",
        "type": "object"
      },
      "EmailAddress": {
        "additionalProperties": false,
        "description": "A single email participant. ``name`` is optional (many headers carry a\nbare address); ``email`` is required and must look like an address.",
        "properties": {
          "email": {
            "description": "Bare email address, e.g. 'a@b.com'.",
            "title": "Email",
            "type": "string"
          },
          "name": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Display name, e.g. 'Alice Example'.",
            "title": "Name"
          }
        },
        "required": [
          "email"
        ],
        "title": "EmailAddress",
        "type": "object"
      },
      "EmailArchiveRequest": {
        "additionalProperties": false,
        "description": "Archive a message (remove it from the inbox). Requires a confirmation\ntoken minted by POST /v1/email/confirm for ``action='archive'``.",
        "properties": {
          "confirmation_token": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Token from POST /v1/email/confirm. An archive without a valid token for this exact (action='archive', message_id) is rejected (403).",
            "title": "Confirmation Token"
          },
          "message_id": {
            "description": "Provider message id to archive.",
            "title": "Message Id",
            "type": "string"
          },
          "provider": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional provider ('google' / 'microsoft' / 'microsoft_work'), used only when the token carries no binding. A token's bound provider always wins; with two mailboxes connected and neither set, the call is rejected (400).",
            "title": "Provider"
          }
        },
        "required": [
          "message_id"
        ],
        "title": "EmailArchiveRequest",
        "type": "object"
      },
      "EmailArchiveResponse": {
        "additionalProperties": false,
        "description": "Result of an archive — carries the undo handle for the 120s window.",
        "properties": {
          "action_id": {
            "description": "Action-log id for this archive.",
            "title": "Action Id",
            "type": "string"
          },
          "archived": {
            "default": true,
            "description": "Always true on success.",
            "title": "Archived",
            "type": "boolean"
          },
          "batch_id": {
            "description": "Undo handle: pass to POST /v1/email/unarchive within the undo window to restore the message to the inbox.",
            "title": "Batch Id",
            "type": "string"
          },
          "message_id": {
            "description": "The message that was archived.",
            "title": "Message Id",
            "type": "string"
          },
          "post_archive_id": {
            "description": "The message id valid AFTER the archive. For folder-based backends (Outlook) the archive move mints a new id; for Gmail it equals the request id. Surfaced so a caller can track the message post-archive.",
            "title": "Post Archive Id",
            "type": "string"
          },
          "schema_version": {
            "default": "2.14",
            "description": "Echoes the contract version.",
            "title": "Schema Version",
            "type": "string"
          },
          "undo_window_seconds": {
            "description": "Seconds the unarchive handle stays valid.",
            "title": "Undo Window Seconds",
            "type": "integer"
          }
        },
        "required": [
          "message_id",
          "action_id",
          "batch_id",
          "post_archive_id",
          "undo_window_seconds"
        ],
        "title": "EmailArchiveResponse",
        "type": "object"
      },
      "EmailAttentionResponse": {
        "additionalProperties": false,
        "description": "Top-level attention-view response envelope (#2582).",
        "properties": {
          "result": {
            "$ref": "#/components/schemas/EmailAttentionResult",
            "description": "The attention-view envelope."
          },
          "schema_version": {
            "default": "2.14",
            "description": "Echoes the contract version.",
            "title": "Schema Version",
            "type": "string"
          }
        },
        "required": [
          "result"
        ],
        "title": "EmailAttentionResponse",
        "type": "object"
      },
      "EmailAttentionResult": {
        "additionalProperties": false,
        "description": "The merged attention-view envelope — computed on open, then cached.\n\n``items == []`` is NOT itself a \"nothing needs you\" claim — it only means\nnothing surfaced from what was actually scanned. A consumer MUST read\n``coverage`` before asserting the mailbox is clear, and must qualify the\nclaim when ``coverage.scan_truncated`` or ``coverage.degraded`` is set\n(e.g. \"of the 200 most recent\" / \"one mailbox couldn't be scanned\") —\nrendering an empty ``items`` list as an unqualified whole-mailbox claim is\nthe exact defect #2584 fixed one layer down for the pre-scan envelope.",
        "properties": {
          "cache_age_seconds": {
            "default": 0.0,
            "description": "Seconds since ``generated_at``. 0 on a freshly-computed result; positive when served from cache — a renderer must label a nonzero age rather than present a cached result as current.",
            "minimum": 0.0,
            "title": "Cache Age Seconds",
            "type": "number"
          },
          "coverage": {
            "$ref": "#/components/schemas/AttentionCoverage",
            "description": "What this view actually scanned."
          },
          "generated_at": {
            "description": "UTC ISO-8601 timestamp of the underlying scan this result reflects.",
            "title": "Generated At",
            "type": "string"
          },
          "items": {
            "description": "Every surfaced item, unordered across signal types.",
            "items": {
              "$ref": "#/components/schemas/AttentionItem"
            },
            "title": "Items",
            "type": "array"
          },
          "kind": {
            "const": "email_attention",
            "default": "email_attention",
            "description": "Discriminator identifying this envelope shape.",
            "title": "Kind",
            "type": "string"
          },
          "stale": {
            "default": false,
            "description": "True when the cache exceeded its freshness threshold and a live refresh was attempted but failed (e.g. a transient mailbox error) — the result is last-known-good, not current.",
            "title": "Stale",
            "type": "boolean"
          }
        },
        "required": [
          "coverage",
          "generated_at"
        ],
        "title": "EmailAttentionResult",
        "type": "object"
      },
      "EmailBriefingResponse": {
        "additionalProperties": false,
        "description": "Latest scheduled inbox briefing (#1608). LOCAL model — the briefing\npayload itself is the frozen contract's ``EmailPreScanResult``.",
        "properties": {
          "briefing": {
            "$ref": "#/components/schemas/EmailPreScanResult",
            "description": "The email_pre_scan envelope the scheduled run produced."
          },
          "generated_at": {
            "description": "UTC ISO-8601 timestamp of the scheduled run.",
            "title": "Generated At",
            "type": "string"
          },
          "schema_version": {
            "default": "2.14",
            "description": "Echoes the contract version.",
            "title": "Schema Version",
            "type": "string"
          }
        },
        "required": [
          "generated_at",
          "briefing"
        ],
        "title": "EmailBriefingResponse",
        "type": "object"
      },
      "EmailCategory": {
        "description": "The five-bucket triage taxonomy (schema 2.0, #1615). Values MUST match\n``triage_heuristics.ALL_CATEGORIES`` — the contract tests assert this.",
        "enum": [
          "URGENT",
          "NEEDS_RESPONSE",
          "FYI",
          "PROMOTIONAL",
          "PERSONAL"
        ],
        "title": "EmailCategory",
        "type": "string"
      },
      "EmailDraftRequest": {
        "additionalProperties": false,
        "description": "Propose a reply and obtain a confirmation token for it.",
        "properties": {
          "attachments": {
            "description": "Attachments to include on the send (schema 2.2, #1542). The minted confirmation token binds to their content digests, so the send must carry these exact files.",
            "items": {
              "$ref": "#/components/schemas/OutgoingAttachment"
            },
            "title": "Attachments",
            "type": "array"
          },
          "body": {
            "description": "Proposed reply body.",
            "title": "Body",
            "type": "string"
          },
          "provider": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional provider binding ('google', 'microsoft', or 'microsoft_work'). When set, the confirmation token is bound to this provider so the send routes to the correct mailbox even when multiple are connected.",
            "title": "Provider"
          },
          "subject": {
            "description": "Proposed subject line.",
            "title": "Subject",
            "type": "string"
          },
          "to": {
            "description": "Proposed recipients (non-empty).",
            "items": {
              "$ref": "#/components/schemas/EmailAddress"
            },
            "minItems": 1,
            "title": "To",
            "type": "array"
          }
        },
        "required": [
          "to",
          "subject",
          "body"
        ],
        "title": "EmailDraftRequest",
        "type": "object"
      },
      "EmailDraftResponse": {
        "additionalProperties": false,
        "description": "The proposed reply plus a single-use confirmation token bound to it.",
        "properties": {
          "confirmation_token": {
            "description": "Echo this back to POST /v1/email/send to authorize sending exactly this payload. Single-use; bound to (to, subject, body, attachments).",
            "title": "Confirmation Token",
            "type": "string"
          },
          "draft": {
            "$ref": "#/components/schemas/DraftReply",
            "description": "The proposed reply (to / subject / body)."
          }
        },
        "required": [
          "draft",
          "confirmation_token"
        ],
        "title": "EmailDraftResponse",
        "type": "object"
      },
      "EmailMessage": {
        "additionalProperties": false,
        "description": "One email message. Shared by the single-email and thread inputs.",
        "properties": {
          "attachments": {
            "description": "Attachment metadata on this message (schema 2.2, #1542). Metadata only — triage weighs names/types/sizes; content never travels here.",
            "items": {
              "$ref": "#/components/schemas/AttachmentMeta"
            },
            "title": "Attachments",
            "type": "array"
          },
          "bcc": {
            "description": "Blind-carbon-copy recipients.",
            "items": {
              "$ref": "#/components/schemas/EmailAddress"
            },
            "title": "Bcc",
            "type": "array"
          },
          "body": {
            "description": "Plain-text message body to analyze.",
            "title": "Body",
            "type": "string"
          },
          "cc": {
            "description": "Carbon-copy recipients.",
            "items": {
              "$ref": "#/components/schemas/EmailAddress"
            },
            "title": "Cc",
            "type": "array"
          },
          "date": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "ISO-8601 timestamp of the message.",
            "title": "Date"
          },
          "from": {
            "$ref": "#/components/schemas/EmailAddress",
            "description": "Sender. Aliased to 'from' (a Python keyword) on the wire."
          },
          "message_id": {
            "description": "Provider message id (opaque).",
            "title": "Message Id",
            "type": "string"
          },
          "subject": {
            "default": "",
            "description": "Subject line.",
            "title": "Subject",
            "type": "string"
          },
          "thread_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Provider thread id this message belongs to.",
            "title": "Thread Id"
          },
          "to": {
            "description": "Primary recipients (the 'To' line).",
            "items": {
              "$ref": "#/components/schemas/EmailAddress"
            },
            "title": "To",
            "type": "array"
          }
        },
        "required": [
          "message_id",
          "from",
          "body"
        ],
        "title": "EmailMessage",
        "type": "object"
      },
      "EmailPreScanRequest": {
        "additionalProperties": false,
        "description": "Request envelope for an inbox pre-scan (#1778). Read-only.",
        "properties": {
          "max_messages": {
            "default": 50,
            "description": "How many recent inbox messages to scan. Bounded so a caller can't request an unbounded mailbox sweep.",
            "maximum": 100.0,
            "minimum": 1.0,
            "title": "Max Messages",
            "type": "integer"
          },
          "schema_version": {
            "default": "2.14",
            "description": "Contract version. Mismatch lets a consumer fail loudly.",
            "title": "Schema Version",
            "type": "string"
          }
        },
        "title": "EmailPreScanRequest",
        "type": "object"
      },
      "EmailPreScanResponse": {
        "additionalProperties": false,
        "description": "Top-level pre-scan response envelope (#1778).",
        "properties": {
          "result": {
            "$ref": "#/components/schemas/EmailPreScanResult",
            "description": "The pre-scan envelope."
          },
          "schema_version": {
            "default": "2.14",
            "description": "Echoes the contract version.",
            "title": "Schema Version",
            "type": "string"
          }
        },
        "required": [
          "result"
        ],
        "title": "EmailPreScanResponse",
        "type": "object"
      },
      "EmailPreScanResult": {
        "additionalProperties": false,
        "description": "The aggregate pre-scan envelope the ``EmailPreScanCard`` renders.\n\nThis is the exact payload the chat surface detects via\n``kind == \"email_pre_scan\"`` — produced by the agent's\n``pre_scan_inbox_impl`` (same classification path), not re-implemented here.",
        "properties": {
          "actionable": {
            "description": "Top messages needing a response (capped).",
            "items": {
              "$ref": "#/components/schemas/PreScanItem"
            },
            "title": "Actionable",
            "type": "array"
          },
          "bulk": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/BulkSummary"
              },
              {
                "type": "null"
              }
            ],
            "description": "(#2743) The filtered informational/promotional remainder — a count plus the filter test(s) that produced it."
          },
          "degraded": {
            "default": false,
            "description": "True when at least one connected mailbox could not be scanned (see mailbox_errors) — the surviving mailboxes' results are still shown, but coverage is partial (#2584).",
            "title": "Degraded",
            "type": "boolean"
          },
          "informational": {
            "description": "The informational (FYI/PERSONAL) messages this count represents (#2633) — empty unless the caller passed ``include_informational=True``, so a bare count is never the only way to audit what was filtered.",
            "items": {
              "$ref": "#/components/schemas/PreScanItem"
            },
            "title": "Informational",
            "type": "array"
          },
          "informational_count": {
            "default": 0,
            "description": "Count of informational (FYI/PERSONAL) messages. Empty by default in ``informational`` below (#2633) — request ``include_informational=True`` on the call to get the full list instead of just this count.",
            "title": "Informational Count",
            "type": "integer"
          },
          "kind": {
            "const": "email_pre_scan",
            "default": "email_pre_scan",
            "description": "Discriminator the chat surface detects to render the card.",
            "title": "Kind",
            "type": "string"
          },
          "mailbox_errors": {
            "anyOf": [
              {
                "items": {
                  "$ref": "#/components/schemas/MailboxError"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "description": "Connected mailboxes that failed during this pre-scan, if any (#2584) — surfaced to the caller, not only logged.",
            "title": "Mailbox Errors"
          },
          "needs_review": {
            "description": "Messages the heuristic was NOT confident about (capped, #2584) — surfaced for human review rather than silently filed under a placeholder category guess (e.g. an unconfident FYI or PROMOTIONAL guess). ``why`` carries the heuristic's escalation reason.",
            "items": {
              "$ref": "#/components/schemas/PreScanItem"
            },
            "title": "Needs Review",
            "type": "array"
          },
          "needs_you": {
            "description": "(#2743) The worklist view: a deterministic VIEW over urgent/actionable/needs_review — never a second, independent classification pass — capped at 5, ordered by kind then oldest-first. ``ref`` is stable within this render only.",
            "items": {
              "$ref": "#/components/schemas/NeedsYouItem"
            },
            "title": "Needs You",
            "type": "array"
          },
          "needs_you_total": {
            "default": 0,
            "description": "(#2743) The true count of needs_you candidates before the 5-item cap — lets the renderer say '5 of N' honestly.",
            "title": "Needs You Total",
            "type": "integer"
          },
          "preferences_applied": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/PreScanPreferencesApplied"
              },
              {
                "type": "null"
              }
            ],
            "description": "Session preferences that shaped this pre-scan."
          },
          "scanned": {
            "default": 0,
            "description": "How many messages this pre-scan actually classified (sum across every bucket, including needs_review) — the honest numerator for 'how much of the inbox did we look at' (#2584).",
            "title": "Scanned",
            "type": "integer"
          },
          "suggested_archives": {
            "description": "Promotional / low-priority messages suggested for archive (capped).",
            "items": {
              "$ref": "#/components/schemas/PreScanItem"
            },
            "title": "Suggested Archives",
            "type": "array"
          },
          "suggested_drafts": {
            "description": "Reserved for future LLM-driven draft generation; empty today.",
            "items": {},
            "title": "Suggested Drafts",
            "type": "array"
          },
          "suspicious": {
            "description": "(#2900) Messages flagged ``is_phishing``/``is_spam`` this scan — a VIEW over the same rows already present in ``actionable`` above, captured BEFORE ``actionable``'s 5-item cap so a flagged message ranked past that cap is never silently dropped from this list. Never a second classification pass.",
            "items": {
              "$ref": "#/components/schemas/PreScanItem"
            },
            "title": "Suspicious",
            "type": "array"
          },
          "suspicious_total": {
            "default": 0,
            "description": "(#2900) The true count of flagged messages before ``suspicious``'s own cap — lets a caller state the count verbatim rather than counting the (possibly capped) list.",
            "title": "Suspicious Total",
            "type": "integer"
          },
          "total_inbox": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Exact total INBOX message count (read + unread) in the scanned mailbox(es) — the honest denominator for scan coverage (#2638), now that pre-scan covers all of INBOX, not just unread mail. Gmail reports this via labels().get's messagesTotal — an exact integer, sourced from the SAME call as total_unread (no extra round-trip). Outlook has no equivalent honest source and reports null — never a fabricated page-size number.",
            "title": "Total Inbox"
          },
          "total_unread": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Exact total UNREAD message count in the scanned mailbox(es) — a secondary coverage figure (#2584), NOT the scan-coverage denominator since #2638 (pre-scan now covers read mail too; see total_inbox for that). Gmail reports this via labels().get's messagesUnread — an exact integer, not list_messages's resultSizeEstimate (measured 2.6x off on a real mailbox). Outlook has no equivalent honest source and reports null — never a fabricated page-size number.",
            "title": "Total Unread"
          },
          "totals": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/PreScanTotals"
              },
              {
                "type": "null"
              }
            ],
            "description": "Pre-cap totals per bucket."
          },
          "urgent": {
            "description": "Top urgent messages (capped).",
            "items": {
              "$ref": "#/components/schemas/PreScanItem"
            },
            "title": "Urgent",
            "type": "array"
          }
        },
        "title": "EmailPreScanResult",
        "type": "object"
      },
      "EmailQuarantineRequest": {
        "additionalProperties": false,
        "description": "Quarantine a phishing message (apply GAIA_PHISHING_QUARANTINE + archive).\nRequires a confirmation token for ``action='quarantine'``.\n\nGmail-only: the label-based quarantine and its reversible undo don't map onto\nOutlook's folder moves, so a request that resolves to an Outlook mailbox is\nrejected with 400 rather than performing a move undo can't reverse (#1738).",
        "properties": {
          "confirmation_token": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Token from POST /v1/email/confirm. A quarantine without a valid token for this exact (action='quarantine', message_id) is rejected (403).",
            "title": "Confirmation Token"
          },
          "is_phishing": {
            "description": "Must be true. The action refuses to quarantine a message not flagged as phishing — a safety gate, never silently bypassed.",
            "title": "Is Phishing",
            "type": "boolean"
          },
          "message_id": {
            "description": "Provider message id to quarantine.",
            "title": "Message Id",
            "type": "string"
          },
          "provider": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional provider ('google' / 'microsoft' / 'microsoft_work'), used only when the token carries no binding (see EmailArchiveRequest.provider).",
            "title": "Provider"
          }
        },
        "required": [
          "message_id",
          "is_phishing"
        ],
        "title": "EmailQuarantineRequest",
        "type": "object"
      },
      "EmailQuarantineResponse": {
        "additionalProperties": false,
        "description": "Result of a quarantine — carries the action id for the 120s undo.",
        "properties": {
          "action_id": {
            "description": "Undo handle: pass to POST /v1/email/unquarantine within the undo window to restore the message's prior labels.",
            "title": "Action Id",
            "type": "string"
          },
          "message_id": {
            "description": "The message that was quarantined.",
            "title": "Message Id",
            "type": "string"
          },
          "prior_labels": {
            "description": "The label set restored on undo (recorded pre-quarantine).",
            "items": {
              "type": "string"
            },
            "title": "Prior Labels",
            "type": "array"
          },
          "quarantine_label_id": {
            "description": "Id of the GAIA_PHISHING_QUARANTINE label that was applied.",
            "title": "Quarantine Label Id",
            "type": "string"
          },
          "quarantined": {
            "default": true,
            "description": "Always true on success.",
            "title": "Quarantined",
            "type": "boolean"
          },
          "schema_version": {
            "default": "2.14",
            "description": "Echoes the contract version.",
            "title": "Schema Version",
            "type": "string"
          },
          "undo_window_seconds": {
            "description": "Seconds the unquarantine handle stays valid.",
            "title": "Undo Window Seconds",
            "type": "integer"
          }
        },
        "required": [
          "message_id",
          "action_id",
          "quarantine_label_id",
          "undo_window_seconds"
        ],
        "title": "EmailQuarantineResponse",
        "type": "object"
      },
      "EmailSearchRequest": {
        "additionalProperties": false,
        "description": "Search the connected mailbox for messages by Gmail-style query / labels.\n\nRead-only: this lists messages already in the mailbox; it never sends,\nmodifies, or triages. It restores the agent's in-loop inbox-search tool\n(``search_messages``) on the REST contract so the Agent UI can drive it\nthrough the package (#1781).\n\nBoth ``query`` and ``labels`` are optional. A ``query`` searches **all\nmail** (Gmail search semantics); ``labels`` filter to those labels; with\n**neither**, the search lists the INBOX (bounded by ``max_results``).",
        "properties": {
          "labels": {
            "anyOf": [
              {
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "description": "Label ids to filter by (e.g. ['INBOX', 'UNREAD']). Omit to apply no label filter; when query is also omitted, the search defaults to the INBOX.",
            "title": "Labels"
          },
          "max_results": {
            "default": 25,
            "description": "Max messages to return (1-100). Each match is hydrated with a per-message fetch, so the count is capped to bound that fan-out.",
            "maximum": 100.0,
            "minimum": 1.0,
            "title": "Max Results",
            "type": "integer"
          },
          "page_token": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Opaque pagination cursor from a previous response's ``next_page_token``. None fetches the first page.",
            "title": "Page Token"
          },
          "query": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Gmail-style search query (e.g. 'from:alice is:unread'). A query searches all mail; omit it to list by label (or the INBOX when labels are also omitted).",
            "title": "Query"
          },
          "schema_version": {
            "default": "2.14",
            "description": "Contract version. Mismatch lets a consumer fail loudly.",
            "title": "Schema Version",
            "type": "string"
          }
        },
        "title": "EmailSearchRequest",
        "type": "object"
      },
      "EmailSearchResponse": {
        "additionalProperties": false,
        "description": "Top-level inbox-search response envelope (#1781).",
        "properties": {
          "count": {
            "description": "Number of messages returned.",
            "title": "Count",
            "type": "integer"
          },
          "messages": {
            "description": "Matching messages (newest-first).",
            "items": {
              "$ref": "#/components/schemas/EmailSearchResultItem"
            },
            "title": "Messages",
            "type": "array"
          },
          "next_page_token": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Opaque token to fetch the next page, or null when no more.",
            "title": "Next Page Token"
          },
          "query": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Echoes the request query (None when unset).",
            "title": "Query"
          },
          "schema_version": {
            "default": "2.14",
            "description": "Echoes the contract version.",
            "title": "Schema Version",
            "type": "string"
          }
        },
        "required": [
          "count"
        ],
        "title": "EmailSearchResponse",
        "type": "object"
      },
      "EmailSearchResultItem": {
        "additionalProperties": false,
        "description": "One message in a search result — inbox-list metadata, not the full body.\n\nHeader values (``from_``/``to``/``date``) are the raw, provider-decoded\nheader strings exactly as the agent's read tools surface them — not parsed\ninto ``EmailAddress`` objects, because list headers can carry multiple or\nmalformed addresses a strict parse would reject. Fetch the full message via\nthe triage path when you need the body.",
        "properties": {
          "date": {
            "default": "",
            "description": "Raw 'Date' header string.",
            "title": "Date",
            "type": "string"
          },
          "from": {
            "default": "",
            "description": "Raw 'From' header string. Aliased to 'from' on the wire.",
            "title": "From",
            "type": "string"
          },
          "id": {
            "description": "Provider message id (opaque).",
            "title": "Id",
            "type": "string"
          },
          "label_ids": {
            "description": "Label ids on the message.",
            "items": {
              "type": "string"
            },
            "title": "Label Ids",
            "type": "array"
          },
          "snippet": {
            "default": "",
            "description": "Provider-supplied short preview of the body.",
            "title": "Snippet",
            "type": "string"
          },
          "subject": {
            "default": "",
            "description": "Subject line.",
            "title": "Subject",
            "type": "string"
          },
          "thread_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Provider thread id this message belongs to.",
            "title": "Thread Id"
          },
          "to": {
            "default": "",
            "description": "Raw 'To' header string.",
            "title": "To",
            "type": "string"
          }
        },
        "required": [
          "id"
        ],
        "title": "EmailSearchResultItem",
        "type": "object"
      },
      "EmailSendRequest": {
        "additionalProperties": false,
        "description": "Send a reply. Requires a valid confirmation token for this payload.",
        "properties": {
          "attachments": {
            "description": "Attachments to send (schema 2.2, #1542). Must match the set the confirmation token was minted for — filename, MIME type, and content digest all bind.",
            "items": {
              "$ref": "#/components/schemas/OutgoingAttachment"
            },
            "title": "Attachments",
            "type": "array"
          },
          "body": {
            "description": "Reply body.",
            "title": "Body",
            "type": "string"
          },
          "confirmation_token": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Confirmation token from POST /v1/email/draft. A send without a valid token for this exact payload is rejected (403).",
            "title": "Confirmation Token"
          },
          "provider": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional provider ('google', 'microsoft', or 'microsoft_work'), used ONLY as the fallback when the confirmation token carries no provider binding. A token's bound provider always wins; with two mailboxes connected and neither a binding nor this field set, the send is rejected as ambiguous (400).",
            "title": "Provider"
          },
          "subject": {
            "description": "Subject line.",
            "title": "Subject",
            "type": "string"
          },
          "to": {
            "description": "Recipients (non-empty).",
            "items": {
              "$ref": "#/components/schemas/EmailAddress"
            },
            "minItems": 1,
            "title": "To",
            "type": "array"
          }
        },
        "required": [
          "to",
          "subject",
          "body"
        ],
        "title": "EmailSendRequest",
        "type": "object"
      },
      "EmailSendResponse": {
        "additionalProperties": false,
        "properties": {
          "attachments": {
            "description": "Metadata of the attachments that went out (schema 2.2).",
            "items": {
              "$ref": "#/components/schemas/AttachmentMeta"
            },
            "title": "Attachments",
            "type": "array"
          },
          "sent": {
            "default": true,
            "description": "Always true on success.",
            "title": "Sent",
            "type": "boolean"
          },
          "sent_id": {
            "description": "Provider message id of the sent email.",
            "title": "Sent Id",
            "type": "string"
          },
          "subject": {
            "description": "Subject of the sent message.",
            "title": "Subject",
            "type": "string"
          },
          "to": {
            "description": "Recipients the message was sent to.",
            "items": {
              "$ref": "#/components/schemas/EmailAddress"
            },
            "title": "To",
            "type": "array"
          }
        },
        "required": [
          "sent_id",
          "to",
          "subject"
        ],
        "title": "EmailSendResponse",
        "type": "object"
      },
      "EmailTriageRequest": {
        "additionalProperties": false,
        "description": "Top-level request envelope shared by REST (#1229) and MCP stdio (#1104).",
        "properties": {
          "context": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TriageContext"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional context (people/projects/tone/self-email) that biases categorization and summary. Absent → behavior unchanged."
          },
          "payload": {
            "description": "The single-email or full-thread input.",
            "discriminator": {
              "mapping": {
                "single": "#/components/schemas/SingleEmailInput",
                "thread": "#/components/schemas/ThreadInput"
              },
              "propertyName": "kind"
            },
            "oneOf": [
              {
                "$ref": "#/components/schemas/SingleEmailInput"
              },
              {
                "$ref": "#/components/schemas/ThreadInput"
              }
            ],
            "title": "Payload"
          },
          "schema_version": {
            "default": "2.14",
            "description": "Contract version. Mismatch lets a consumer fail loudly.",
            "title": "Schema Version",
            "type": "string"
          }
        },
        "required": [
          "payload"
        ],
        "title": "EmailTriageRequest",
        "type": "object"
      },
      "EmailTriageResponse": {
        "additionalProperties": false,
        "description": "Top-level response envelope shared by REST (#1229) and MCP stdio (#1104).",
        "properties": {
          "request_kind": {
            "description": "Which input shape produced this result.",
            "enum": [
              "single",
              "thread"
            ],
            "title": "Request Kind",
            "type": "string"
          },
          "result": {
            "$ref": "#/components/schemas/EmailTriageResult",
            "description": "The structured analysis."
          },
          "schema_version": {
            "default": "2.14",
            "description": "Echoes the contract version.",
            "title": "Schema Version",
            "type": "string"
          }
        },
        "required": [
          "request_kind",
          "result"
        ],
        "title": "EmailTriageResponse",
        "type": "object"
      },
      "EmailTriageResult": {
        "additionalProperties": false,
        "description": "The structured analysis of a single email or thread.",
        "properties": {
          "action_items": {
            "description": "Extracted actions (may be empty).",
            "items": {
              "$ref": "#/components/schemas/ActionItem"
            },
            "title": "Action Items",
            "type": "array"
          },
          "attachments": {
            "description": "Attachment metadata of the analyzed message (or every message in the thread, oldest-first), echoed for downstream processing (schema 2.2, #1542).",
            "items": {
              "$ref": "#/components/schemas/AttachmentMeta"
            },
            "title": "Attachments",
            "type": "array"
          },
          "category": {
            "$ref": "#/components/schemas/EmailCategory",
            "description": "One of the five taxonomy buckets (schema 2.0)."
          },
          "draft": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/DraftScaffold"
              },
              {
                "type": "null"
              }
            ],
            "description": "Proposed reply SCAFFOLD (recipient + subject only, no body), or null when no reply is suggested (schema 2.3). Triage never composes reply prose; to send a reply, compose the body and POST it to /v1/email/draft."
          },
          "is_phishing": {
            "default": false,
            "description": "Phishing signal (independent of spam).",
            "title": "Is Phishing",
            "type": "boolean"
          },
          "is_spam": {
            "default": false,
            "description": "Spam signal (independent).",
            "title": "Is Spam",
            "type": "boolean"
          },
          "message_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Echoes the provider message-id from the request (SingleEmailInput.message or ThreadInput.thread_id). Null when the result was produced from a raw Gmail-API message (no contract message_id available).",
            "title": "Message Id"
          },
          "suggested_action": {
            "default": "none",
            "description": "Suggested next action: reply (URGENT/NEEDS_RESPONSE), archive (PROMOTIONAL), or none (FYI/PERSONAL). Derived by precedence rule -- never required so existing consumers are unaffected.",
            "enum": [
              "reply",
              "none",
              "archive"
            ],
            "title": "Suggested Action",
            "type": "string"
          },
          "summary": {
            "description": "Plain-text summary of the email/thread.",
            "title": "Summary",
            "type": "string"
          },
          "usage": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/TriageUsage"
              },
              {
                "type": "null"
              }
            ],
            "description": "LLM usage metrics (tokens + aggregate TPS) for this triage. Null on the heuristic-only path where no LLM call was made. Compare prompt_tokens against the agent's context-window envelope (16K target / 32K max, #1892) to see how much of the window a payload consumed."
          }
        },
        "required": [
          "category",
          "summary"
        ],
        "title": "EmailTriageResult",
        "type": "object"
      },
      "EmailUnarchiveRequest": {
        "additionalProperties": false,
        "description": "Reverse an archive within the undo window. NOT gated — it restores.",
        "properties": {
          "batch_id": {
            "description": "The undo handle returned by POST /v1/email/archive.",
            "title": "Batch Id",
            "type": "string"
          },
          "provider": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional provider ('google' / 'microsoft' / 'microsoft_work'). Omit to route by the mailbox recorded at archive time (the default and correct choice).",
            "title": "Provider"
          }
        },
        "required": [
          "batch_id"
        ],
        "title": "EmailUnarchiveRequest",
        "type": "object"
      },
      "EmailUnarchiveResponse": {
        "additionalProperties": false,
        "description": "Result of an unarchive — partial success is reported, never silent.",
        "properties": {
          "batch_id": {
            "description": "The undo handle that was processed.",
            "title": "Batch Id",
            "type": "string"
          },
          "failed": {
            "description": "Messages that could not be restored (with reasons).",
            "items": {
              "$ref": "#/components/schemas/UnarchiveFailure"
            },
            "title": "Failed",
            "type": "array"
          },
          "messages": {
            "description": "Each restored message.",
            "items": {
              "$ref": "#/components/schemas/UnarchivedMessage"
            },
            "title": "Messages",
            "type": "array"
          },
          "restored": {
            "description": "Count of messages restored to inbox.",
            "title": "Restored",
            "type": "integer"
          },
          "schema_version": {
            "default": "2.14",
            "description": "Echoes the contract version.",
            "title": "Schema Version",
            "type": "string"
          },
          "undone": {
            "default": true,
            "description": "True when at least one restored.",
            "title": "Undone",
            "type": "boolean"
          }
        },
        "required": [
          "batch_id",
          "restored"
        ],
        "title": "EmailUnarchiveResponse",
        "type": "object"
      },
      "EmailUnquarantineRequest": {
        "additionalProperties": false,
        "description": "Reverse a quarantine within the undo window. NOT gated — it restores.",
        "properties": {
          "action_id": {
            "description": "The action id returned by POST /v1/email/quarantine.",
            "title": "Action Id",
            "type": "string"
          },
          "provider": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Optional provider ('google' / 'microsoft' / 'microsoft_work'); omit to route by the mailbox recorded at quarantine time.",
            "title": "Provider"
          }
        },
        "required": [
          "action_id"
        ],
        "title": "EmailUnquarantineRequest",
        "type": "object"
      },
      "EmailUnquarantineResponse": {
        "additionalProperties": false,
        "description": "Result of an unquarantine.",
        "properties": {
          "action_id": {
            "description": "The action id that was undone.",
            "title": "Action Id",
            "type": "string"
          },
          "message_id": {
            "description": "The restored message id.",
            "title": "Message Id",
            "type": "string"
          },
          "restored": {
            "default": true,
            "description": "Always true on success.",
            "title": "Restored",
            "type": "boolean"
          },
          "schema_version": {
            "default": "2.14",
            "description": "Echoes the contract version.",
            "title": "Schema Version",
            "type": "string"
          }
        },
        "required": [
          "action_id",
          "message_id"
        ],
        "title": "EmailUnquarantineResponse",
        "type": "object"
      },
      "ForwardedConnectionRequest": {
        "additionalProperties": false,
        "description": "A short-lived connector access token forwarded by the daemon (#2154).\n\nDeliberately carries NO ``refresh_token`` and NO ``client_secret`` — the\ndaemon owns refresh; forwarding either across the process boundary is the\nexact posture forward-out exists to prevent. The provider is the path\nparameter, so it is not repeated in the body.",
        "properties": {
          "access_token": {
            "description": "The short-lived OAuth access token (bearer). Non-empty.",
            "minLength": 1,
            "title": "Access Token",
            "type": "string"
          },
          "account_email": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Mailbox address the token is for (display only), if known.",
            "title": "Account Email"
          },
          "expires_at": {
            "description": "Wall-clock UNIX expiry of the access token, so the sidecar knows when to expect a re-forward. Must be positive.",
            "exclusiveMinimum": 0.0,
            "title": "Expires At",
            "type": "number"
          },
          "scopes": {
            "description": "Scopes this access token carries (the granted set).",
            "items": {
              "type": "string"
            },
            "title": "Scopes",
            "type": "array"
          }
        },
        "required": [
          "access_token",
          "expires_at"
        ],
        "title": "ForwardedConnectionRequest",
        "type": "object"
      },
      "ForwardedConnectionSummary": {
        "additionalProperties": false,
        "description": "Metadata echo for a forwarded connection. NEVER includes the token.",
        "properties": {
          "account_email": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Mailbox address (display only), or null.",
            "title": "Account Email"
          },
          "expires_at": {
            "description": "Wall-clock UNIX expiry of the forwarded access token.",
            "title": "Expires At",
            "type": "number"
          },
          "forwarded": {
            "default": true,
            "description": "Always true — this is a forwarded credential.",
            "title": "Forwarded",
            "type": "boolean"
          },
          "provider": {
            "description": "Connector provider id, e.g. 'google'.",
            "title": "Provider",
            "type": "string"
          },
          "scopes": {
            "description": "Scopes the forwarded token carries.",
            "items": {
              "type": "string"
            },
            "title": "Scopes",
            "type": "array"
          }
        },
        "required": [
          "provider",
          "expires_at"
        ],
        "title": "ForwardedConnectionSummary",
        "type": "object"
      },
      "ForwardedConnectionWithdrawResponse": {
        "additionalProperties": false,
        "description": "Result of withdrawing a forwarded connection (#2154). Idempotent —\n``withdrawn`` is False when there was nothing to drop.",
        "properties": {
          "provider": {
            "description": "Connector provider id that was targeted.",
            "title": "Provider",
            "type": "string"
          },
          "withdrawn": {
            "description": "True when a forwarded credential was removed, else False.",
            "title": "Withdrawn",
            "type": "boolean"
          }
        },
        "required": [
          "provider",
          "withdrawn"
        ],
        "title": "ForwardedConnectionWithdrawResponse",
        "type": "object"
      },
      "ForwardedConnectionsResponse": {
        "additionalProperties": false,
        "description": "Metadata-only list of the sidecar's forwarded connections (#2154).",
        "properties": {
          "connections": {
            "description": "One entry per forwarded connection.",
            "items": {
              "$ref": "#/components/schemas/ForwardedConnectionSummary"
            },
            "title": "Connections",
            "type": "array"
          },
          "schema_version": {
            "default": "2.14",
            "description": "Echoes the contract version.",
            "title": "Schema Version",
            "type": "string"
          }
        },
        "title": "ForwardedConnectionsResponse",
        "type": "object"
      },
      "HTTPValidationError": {
        "properties": {
          "detail": {
            "items": {
              "$ref": "#/components/schemas/ValidationError"
            },
            "title": "Detail",
            "type": "array"
          }
        },
        "title": "HTTPValidationError",
        "type": "object"
      },
      "HealthResponse": {
        "additionalProperties": false,
        "description": "Liveness/readiness probe for the email surface.\n\nDependency-light by design: it never touches a live mailbox or the LLM, so a\nhost can confirm the router is mounted and serving before any connector or\nmodel is configured.",
        "properties": {
          "service": {
            "const": "gaia-agent-email",
            "default": "gaia-agent-email",
            "description": "Stable service identifier.",
            "title": "Service",
            "type": "string"
          },
          "status": {
            "const": "ok",
            "default": "ok",
            "description": "Always 'ok' when the surface is serving.",
            "title": "Status",
            "type": "string"
          }
        },
        "title": "HealthResponse",
        "type": "object"
      },
      "InitLemonadeStatus": {
        "additionalProperties": false,
        "description": "Reachability AND version-compatibility of the local Lemonade Server.",
        "properties": {
          "base_url": {
            "description": "The /api/v1 base URL that was probed.",
            "title": "Base Url",
            "type": "string"
          },
          "compatible": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "description": "True when version >= min_version. Null when the version could not be determined (the check was indeterminate, not a pass).",
            "title": "Compatible"
          },
          "min_version": {
            "description": "Minimum Lemonade version the triage stack requires (gaia_agent_email.version.MIN_LEMONADE_VERSION).",
            "title": "Min Version",
            "type": "string"
          },
          "reachable": {
            "description": "True when Lemonade answered the /health probe.",
            "title": "Reachable",
            "type": "boolean"
          },
          "version": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Lemonade's self-reported server version (from /health). Null when the server doesn't advertise one.",
            "title": "Version"
          }
        },
        "required": [
          "reachable",
          "base_url",
          "min_version"
        ],
        "title": "InitLemonadeStatus",
        "type": "object"
      },
      "InitModelStatus": {
        "additionalProperties": false,
        "description": "Presence (and, when cheap, loadability) of the triage model.",
        "properties": {
          "ctx_size": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "The context window the model is CURRENTLY loaded at, as reported by the server's /health (recipe_options.ctx_size). Null whenever the model is not loaded right now or the server does not report a ctx — never a config echo or a guess (#1892). Compare against the envelope (16384 target / 32768 max) to see what window a run would actually measure.",
            "title": "Ctx Size"
          },
          "id": {
            "description": "Resolved Lemonade model id for triage.",
            "title": "Id",
            "type": "string"
          },
          "loadable": {
            "anyOf": [
              {
                "type": "boolean"
              },
              {
                "type": "null"
              }
            ],
            "description": "Whether the model actually loads. Not probed in v1 (forcing a load is heavy), so this is null — `present` is the readiness signal. Reserved for an opt-in deeper check.",
            "title": "Loadable"
          },
          "present": {
            "description": "True when the model is downloaded on the server.",
            "title": "Present",
            "type": "boolean"
          }
        },
        "required": [
          "id",
          "present"
        ],
        "title": "InitModelStatus",
        "type": "object"
      },
      "InitResponse": {
        "additionalProperties": false,
        "description": "Readiness preflight for the whole triage stack (#1795).\n\n``ready`` is True only when Lemonade is reachable AND the triage model is\npresent. The route returns HTTP 200 when ready and 503 when not, with an\nactionable ``hint`` naming what to fix — so an integrator can verify \"ready\nto triage,\" not just \"process up.\" Read-only: probes only, no model pull.",
        "properties": {
          "hint": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Actionable next step when not ready (what failed / what to do). Null when ready.",
            "title": "Hint"
          },
          "lemonade": {
            "$ref": "#/components/schemas/InitLemonadeStatus",
            "description": "Lemonade Server reachability."
          },
          "model": {
            "$ref": "#/components/schemas/InitModelStatus",
            "description": "Triage model status."
          },
          "ready": {
            "description": "True when the triage stack is ready to serve.",
            "title": "Ready",
            "type": "boolean"
          }
        },
        "required": [
          "ready",
          "lemonade",
          "model"
        ],
        "title": "InitResponse",
        "type": "object"
      },
      "MailboxError": {
        "additionalProperties": false,
        "description": "One connected mailbox that failed during a pre-scan (#2584).\n\nA partial failure never silently shrinks the result — every skipped\nmailbox is named here so the caller can tell \"empty inbox\" apart from\n\"half the inbox couldn't be scanned\".",
        "properties": {
          "error": {
            "description": "Actionable error message for the failure.",
            "title": "Error",
            "type": "string"
          },
          "mailbox": {
            "description": "Provider name of the failed mailbox ('google' / 'microsoft' / 'microsoft_work').",
            "title": "Mailbox",
            "type": "string"
          }
        },
        "required": [
          "mailbox",
          "error"
        ],
        "title": "MailboxError",
        "type": "object"
      },
      "MessageError": {
        "additionalProperties": false,
        "description": "One message that could not be fetched during a scan (#2716).\n\nDistinct from ``MailboxError``: a rate-limited message is NOT a mailbox\nfailure — every other message in that mailbox's scan is still present\nin the result. Not reusable as ``MailboxError`` since that model has no\n``message_id`` field.",
        "properties": {
          "error": {
            "description": "Actionable error message for the failure.",
            "title": "Error",
            "type": "string"
          },
          "message_id": {
            "description": "Provider message id that failed.",
            "title": "Message Id",
            "type": "string"
          }
        },
        "required": [
          "message_id",
          "error"
        ],
        "title": "MessageError",
        "type": "object"
      },
      "NeedsYouItem": {
        "additionalProperties": false,
        "description": "One thing a human must act on (#2743) — a VIEW over the already-\nclassified urgent/actionable/needs_review buckets PLUS the waiting-on-you\ndetector and persisted action items, never re-derived from raw scan\nresults (see the module's 2.11 changelog entry above).\n\n``kind`` reuses the published :class:`AttentionItemKind` rather than a\nparallel verb enum — the renderer maps ``kind`` to a verb label\n(REPLY/DECIDE/CHECK) at render time; the wire only carries the source\nsignal, so a category-classified item is never mislabeled with the\ndetector's own ``waiting_on_you`` value.",
        "properties": {
          "age_seconds": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Seconds since the message was received, when known.",
            "title": "Age Seconds"
          },
          "detail": {
            "description": "Reserved for up to two lines of real substance (the question actually asked, the meeting time actually proposed, the deadline actually quoted). ALWAYS EMPTY today — the per-item LLM extraction pass that would fill it was implemented and then withdrawn from #2743 before merge (see commit 25738509 for the working extraction + injection-defense wrapping a follow-up issue will build on). The field ships now, empty, so populating it later is additive rather than a second contract bump.",
            "items": {
              "type": "string"
            },
            "maxItems": 2,
            "title": "Detail",
            "type": "array"
          },
          "due_hint": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Free-text due hint (action items only); null otherwise.",
            "title": "Due Hint"
          },
          "kind": {
            "$ref": "#/components/schemas/AttentionItemKind",
            "description": "Which signal surfaced this item."
          },
          "mailbox": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Provider name ('google' / 'microsoft' / 'microsoft_work') this item came from. Set only when more than one mailbox is connected.",
            "title": "Mailbox"
          },
          "message_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Provider message id (opaque). None only for an action item carried from a prior triage with no recoverable source message.",
            "title": "Message Id"
          },
          "ref": {
            "description": "1-based row number, stable within ONE card render only — a rescan re-orders and renumbers by design (older mail found by a deeper scan sorts to the front).",
            "minimum": 1.0,
            "title": "Ref",
            "type": "integer"
          },
          "sender": {
            "default": "",
            "description": "Raw 'From' header of the source message.",
            "title": "Sender",
            "type": "string"
          },
          "subject": {
            "default": "",
            "description": "Subject line of the source message.",
            "title": "Subject",
            "type": "string"
          },
          "thread_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Provider thread id, when known.",
            "title": "Thread Id"
          },
          "why": {
            "description": "Plain-language reason this item needs attention.",
            "title": "Why",
            "type": "string"
          }
        },
        "required": [
          "ref",
          "kind",
          "why"
        ],
        "title": "NeedsYouItem",
        "type": "object"
      },
      "OutgoingAttachment": {
        "additionalProperties": false,
        "description": "One attachment to include on a draft/send (schema 2.2, #1542).\n\nContent travels inline as standard (RFC 4648) base64. Validation is\nfail-loud: a non-decodable payload, a malformed MIME type, or a decoded\nsize over ``MAX_ATTACHMENT_BYTES`` is a ``ValidationError`` — never\nsilently truncated or dropped.",
        "properties": {
          "content_base64": {
            "description": "File content, standard base64 (RFC 4648).",
            "title": "Content Base64",
            "type": "string"
          },
          "filename": {
            "description": "Filename shown to the recipient.",
            "title": "Filename",
            "type": "string"
          },
          "mime_type": {
            "description": "MIME type of the content, e.g. 'application/pdf'.",
            "title": "Mime Type",
            "type": "string"
          }
        },
        "required": [
          "filename",
          "mime_type",
          "content_base64"
        ],
        "title": "OutgoingAttachment",
        "type": "object"
      },
      "PreScanItem": {
        "additionalProperties": false,
        "description": "One surfaced inbox message in a pre-scan section.\n\n``why`` carries the rationale for urgent/actionable rows; ``reason`` carries\nit for suggested-archive rows. The frontend card reads ``reason ?? why``, so\nexactly one is populated per row depending on the section.",
        "properties": {
          "is_meeting_request": {
            "default": false,
            "description": "True when the heuristic confidently detected a meeting/scheduling request in this message's subject/snippet (#2583). Read-only signal — detection makes no calendar changes.",
            "title": "Is Meeting Request",
            "type": "boolean"
          },
          "is_phishing": {
            "default": false,
            "description": "True when the shared phishing detector (``detect_phishing``) flagged this message (#2900). Previously readable only as a fragment inside ``why`` ('flagged as phishing — ...'); now a real field so a caller can act on it without re-parsing prose.",
            "title": "Is Phishing",
            "type": "boolean"
          },
          "is_spam": {
            "default": false,
            "description": "True when the shared spam heuristic flagged this message (#2900). Same rationale as ``is_phishing`` above — a real field instead of a ``why``-string fragment.",
            "title": "Is Spam",
            "type": "boolean"
          },
          "message_id": {
            "description": "Provider message id (opaque).",
            "title": "Message Id",
            "type": "string"
          },
          "reason": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Rationale for a suggested-archive row (the heuristic reason).",
            "title": "Reason"
          },
          "sender": {
            "default": "",
            "description": "Raw 'From' header of the message (display + address).",
            "title": "Sender",
            "type": "string"
          },
          "subject": {
            "default": "",
            "description": "Subject line.",
            "title": "Subject",
            "type": "string"
          },
          "thread_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Provider thread id this message belongs to.",
            "title": "Thread Id"
          },
          "why": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Rationale for an urgent/actionable row (the heuristic reason).",
            "title": "Why"
          }
        },
        "required": [
          "message_id"
        ],
        "title": "PreScanItem",
        "type": "object"
      },
      "PreScanPreferencesApplied": {
        "additionalProperties": false,
        "description": "The session sender/category preferences that shaped this pre-scan.\n\nAlways present (with empty defaults) so the frontend schema can lock in now;\nthe stateless REST surface leaves these empty today, but the field carries a\nstable shape for a future preference-aware caller.",
        "properties": {
          "category_defaults": {
            "additionalProperties": {
              "type": "string"
            },
            "description": "Per-category default action (e.g. {'FYI': 'archive'}).",
            "title": "Category Defaults",
            "type": "object"
          },
          "low_priority_senders": {
            "description": "Senders always treated as low-priority.",
            "items": {
              "type": "string"
            },
            "title": "Low Priority Senders",
            "type": "array"
          },
          "priority_senders": {
            "description": "Senders surfaced/ordered ahead of others (#2632); does not change category — content still decides urgency.",
            "items": {
              "type": "string"
            },
            "title": "Priority Senders",
            "type": "array"
          }
        },
        "title": "PreScanPreferencesApplied",
        "type": "object"
      },
      "PreScanTotals": {
        "additionalProperties": false,
        "description": "Pre-cap counts per bucket — the true totals before the per-section caps\nare applied to the surfaced lists. Lets the card show \"N surfaced\" honestly.",
        "properties": {
          "actionable": {
            "default": 0,
            "description": "Total actionable messages found.",
            "title": "Actionable",
            "type": "integer"
          },
          "informational": {
            "default": 0,
            "description": "Total informational (FYI/PERSONAL) messages found.",
            "title": "Informational",
            "type": "integer"
          },
          "needs_review": {
            "default": 0,
            "description": "Total messages the heuristic was NOT confident about (#2584) — surfaced for human review rather than filed under a placeholder category guess.",
            "title": "Needs Review",
            "type": "integer"
          },
          "suggested_archives": {
            "default": 0,
            "description": "Total suggested-archive messages found.",
            "title": "Suggested Archives",
            "type": "integer"
          },
          "urgent": {
            "default": 0,
            "description": "Total urgent messages found.",
            "title": "Urgent",
            "type": "integer"
          }
        },
        "title": "PreScanTotals",
        "type": "object"
      },
      "QueryCancelResponse": {
        "additionalProperties": false,
        "description": "Result of ``POST /v1/email/query/{run_id}/cancel``.",
        "properties": {
          "cancelled": {
            "default": true,
            "description": "True once the cancel was delivered to the run.",
            "title": "Cancelled",
            "type": "boolean"
          },
          "run_id": {
            "description": "The run that was signalled to cancel.",
            "title": "Run Id",
            "type": "string"
          },
          "status": {
            "default": "ok",
            "description": "Always 'ok' on success.",
            "title": "Status",
            "type": "string"
          }
        },
        "required": [
          "run_id"
        ],
        "title": "QueryCancelResponse",
        "type": "object"
      },
      "QueryContextItem": {
        "additionalProperties": false,
        "description": "One prior turn pushed in the request body (spec §2.4).",
        "properties": {
          "content": {
            "description": "The message text for this turn.",
            "title": "Content",
            "type": "string"
          },
          "role": {
            "description": "Transcript role: 'user', 'assistant', 'system', or 'tool'.",
            "title": "Role",
            "type": "string"
          }
        },
        "required": [
          "role",
          "content"
        ],
        "title": "QueryContextItem",
        "type": "object"
      },
      "QueryRequest": {
        "additionalProperties": false,
        "description": "``POST /v1/email/query`` request body (frozen #2015 contract, spec §2.2).",
        "properties": {
          "can_answer_questions": {
            "default": false,
            "description": "Whether THIS caller can render a 'needs_input' event and POST the answer back to /query/{run_id}/respond. Defaults to false, which is the safe answer: a caller that cannot answer would otherwise park the run until the question times out, which reads as a hang. When false, a step that would ask instead fails immediately with what the user should do on this surface.",
            "title": "Can Answer Questions",
            "type": "boolean"
          },
          "context": {
            "description": "The relevant transcript slice, pushed in the body. May be an empty array for a fresh conversation, but the field must be present.",
            "items": {
              "$ref": "#/components/schemas/QueryContextItem"
            },
            "title": "Context",
            "type": "array"
          },
          "max_steps": {
            "anyOf": [
              {
                "minimum": 1.0,
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Agent-loop step ceiling. Omitted → the agent's configured default.",
            "title": "Max Steps"
          },
          "model": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Model id override. Omitted → the sidecar's default.",
            "title": "Model"
          },
          "provider": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "LLM provider override. The email agent runs local inference only, so only 'lemonade' is accepted; any other value is rejected (400).",
            "title": "Provider"
          },
          "query": {
            "description": "The natural-language request driving the agent loop.",
            "minLength": 1,
            "title": "Query",
            "type": "string"
          },
          "run_id": {
            "description": "Host-minted streaming-run handle (UUIDv4). Cancellation (POST /v1/email/query/{run_id}/cancel) keys off it, so the run is cancellable from the instant the request is sent.",
            "title": "Run Id",
            "type": "string"
          },
          "session_id": {
            "anyOf": [
              {
                "pattern": "^[A-Za-z0-9_-]{1,128}$",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Opaque conversation id, minted by the host once per conversation (#2829). Omitted -> a fresh throwaway agent per call, exactly like before. Present -> the run resolves the SAME agent used by every other turn on this id (via the session-scoped registry), so a reference to something an earlier turn surfaced can resolve. Constrained to the charset a dict key / URL path segment / log line tolerates.",
            "title": "Session Id"
          }
        },
        "required": [
          "query",
          "run_id",
          "context"
        ],
        "title": "QueryRequest",
        "type": "object"
      },
      "QueryRespondRequest": {
        "additionalProperties": false,
        "description": "Body of ``POST /v1/email/query/{run_id}/respond`` (spec §5.1).",
        "properties": {
          "request_id": {
            "description": "The 'request_id' from the needs_input event being answered. An answer for a question that is no longer pending is rejected (409) rather than applied to whatever the run is waiting on now.",
            "minLength": 1,
            "title": "Request Id",
            "type": "string"
          },
          "value": {
            "description": "The answer: an option's 'value' (or its 'label'), or free text when the question set allow_free_text.",
            "minLength": 1,
            "title": "Value",
            "type": "string"
          }
        },
        "required": [
          "request_id",
          "value"
        ],
        "title": "QueryRespondRequest",
        "type": "object"
      },
      "QueryRespondResponse": {
        "additionalProperties": false,
        "description": "Result of ``POST /v1/email/query/{run_id}/respond``.",
        "properties": {
          "accepted": {
            "default": true,
            "description": "True once the answer unblocked the run.",
            "title": "Accepted",
            "type": "boolean"
          },
          "request_id": {
            "description": "The question that was answered.",
            "title": "Request Id",
            "type": "string"
          },
          "run_id": {
            "description": "The run the answer was delivered to.",
            "title": "Run Id",
            "type": "string"
          },
          "status": {
            "default": "ok",
            "description": "Always 'ok' on success.",
            "title": "Status",
            "type": "string"
          }
        },
        "required": [
          "run_id",
          "request_id"
        ],
        "title": "QueryRespondResponse",
        "type": "object"
      },
      "SingleEmailInput": {
        "additionalProperties": false,
        "description": "A single email to triage (AC3 — single-email path).",
        "properties": {
          "kind": {
            "const": "single",
            "default": "single",
            "title": "Kind",
            "type": "string"
          },
          "message": {
            "$ref": "#/components/schemas/EmailMessage",
            "description": "The one message to analyze."
          },
          "principal": {
            "$ref": "#/components/schemas/EmailAddress",
            "description": "The recipient the agent acts on behalf of — whose inbox this is. Distinct from per-message 'to': the principal is the account owner, not necessarily a recipient of every message in a thread."
          }
        },
        "required": [
          "principal",
          "message"
        ],
        "title": "SingleEmailInput",
        "type": "object"
      },
      "ThreadInput": {
        "additionalProperties": false,
        "description": "A full conversation thread to triage (AC3 — thread path).",
        "properties": {
          "kind": {
            "const": "thread",
            "default": "thread",
            "title": "Kind",
            "type": "string"
          },
          "messages": {
            "description": "Every message in the thread, oldest-first. Non-empty.",
            "items": {
              "$ref": "#/components/schemas/EmailMessage"
            },
            "minItems": 1,
            "title": "Messages",
            "type": "array"
          },
          "principal": {
            "$ref": "#/components/schemas/EmailAddress",
            "description": "The recipient the agent acts on behalf of — whose inbox this is. Distinct from per-message 'to': the principal is the account owner, not necessarily a recipient of every message in a thread."
          },
          "thread_id": {
            "description": "Provider thread id for the conversation.",
            "title": "Thread Id",
            "type": "string"
          }
        },
        "required": [
          "principal",
          "thread_id",
          "messages"
        ],
        "title": "ThreadInput",
        "type": "object"
      },
      "TriageContext": {
        "additionalProperties": false,
        "description": "Optional caller-supplied context that biases categorization/summary\n(#1541). Absent → behavior is identical to today. Coexists with gaia\nmemory (#1114) without requiring it.",
        "properties": {
          "people": {
            "description": "Important people whose mail should weigh higher.",
            "items": {
              "type": "string"
            },
            "title": "People",
            "type": "array"
          },
          "projects": {
            "description": "Active projects the principal cares about.",
            "items": {
              "type": "string"
            },
            "title": "Projects",
            "type": "array"
          },
          "self_email": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The principal's own address, so the model knows who 'I' is.",
            "title": "Self Email"
          },
          "tone": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Preferred summary tone, e.g. 'concise'.",
            "title": "Tone"
          }
        },
        "title": "TriageContext",
        "type": "object"
      },
      "TriageUsage": {
        "additionalProperties": false,
        "description": "LLM usage metrics for a triage, aggregated across the classify +\nsummarize calls. Reuses the existing ``AgentResponse.stats`` measurement\n(#1277/#1278) — no new measurement path. ``None`` on the heuristic-only\npath where no LLM call is made.",
        "properties": {
          "completion_tokens": {
            "default": 0,
            "description": "Sum of output (completion) tokens across the LLM calls.",
            "title": "Completion Tokens",
            "type": "integer"
          },
          "prompt_tokens": {
            "default": 0,
            "description": "Sum of input tokens across the LLM calls.",
            "title": "Prompt Tokens",
            "type": "integer"
          },
          "tokens_per_second": {
            "default": 0.0,
            "description": "Aggregate decode throughput (total output tokens / total decode time).",
            "title": "Tokens Per Second",
            "type": "number"
          },
          "total_tokens": {
            "default": 0,
            "description": "Sum of input + output tokens across the LLM calls.",
            "title": "Total Tokens",
            "type": "integer"
          }
        },
        "title": "TriageUsage",
        "type": "object"
      },
      "UnarchiveFailure": {
        "additionalProperties": false,
        "description": "One message in the batch that failed to restore.",
        "properties": {
          "error": {
            "description": "Actionable failure reason.",
            "title": "Error",
            "type": "string"
          },
          "message_id": {
            "description": "The message that failed to restore.",
            "title": "Message Id",
            "type": "string"
          }
        },
        "required": [
          "message_id",
          "error"
        ],
        "title": "UnarchiveFailure",
        "type": "object"
      },
      "UnarchivedMessage": {
        "additionalProperties": false,
        "description": "One message restored to the inbox by an unarchive.",
        "properties": {
          "action_id": {
            "description": "Action-log id that was undone.",
            "title": "Action Id",
            "type": "string"
          },
          "message_id": {
            "description": "The restored message id.",
            "title": "Message Id",
            "type": "string"
          }
        },
        "required": [
          "message_id",
          "action_id"
        ],
        "title": "UnarchivedMessage",
        "type": "object"
      },
      "ValidationError": {
        "properties": {
          "ctx": {
            "title": "Context",
            "type": "object"
          },
          "input": {
            "title": "Input"
          },
          "loc": {
            "items": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ]
            },
            "title": "Location",
            "type": "array"
          },
          "msg": {
            "title": "Message",
            "type": "string"
          },
          "type": {
            "title": "Error Type",
            "type": "string"
          }
        },
        "required": [
          "loc",
          "msg",
          "type"
        ],
        "title": "ValidationError",
        "type": "object"
      },
      "VersionResponse": {
        "additionalProperties": false,
        "description": "The two version numbers a host negotiates against.\n\n``apiVersion`` is the frozen REST/contract version (a contract bump bumps it);\n``agentVersion`` is the package build. Both come from\n``gaia_agent_email.version`` so this endpoint and the freeze server's\n``/version`` report identical values.",
        "properties": {
          "agentVersion": {
            "description": "Package build version.",
            "title": "Agentversion",
            "type": "string"
          },
          "apiVersion": {
            "description": "REST/contract version (contract.SCHEMA_VERSION).",
            "title": "Apiversion",
            "type": "string"
          }
        },
        "required": [
          "apiVersion",
          "agentVersion"
        ],
        "title": "VersionResponse",
        "type": "object"
      }
    },
    "securitySchemes": {
      "bearerAuth": {
        "scheme": "bearer",
        "type": "http"
      }
    }
  },
  "info": {
    "description": "REST surface for the GAIA Email Triage agent (/v1/email/*). Cross-implementation contract for the npm client and native builds.",
    "title": "GAIA Email Agent API",
    "version": "2.14"
  },
  "openapi": "3.1.0",
  "paths": {
    "/v1/connections": {
      "get": {
        "description": "Metadata-only view of the forwarded connections. NEVER returns tokens.",
        "operationId": "list_forwarded_connections_v1_connections_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ForwardedConnectionsResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "List Forwarded Connections",
        "tags": [
          "email-forward-out"
        ]
      }
    },
    "/v1/connections/{provider}": {
      "delete": {
        "description": "Withdraw a forwarded credential (daemon revoke/uninstall). Idempotent.",
        "operationId": "withdraw_forwarded_connection_v1_connections__provider__delete",
        "parameters": [
          {
            "in": "path",
            "name": "provider",
            "required": true,
            "schema": {
              "title": "Provider",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ForwardedConnectionWithdrawResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Withdraw Forwarded Connection",
        "tags": [
          "email-forward-out"
        ]
      },
      "post": {
        "description": "Accept a daemon-forwarded short-lived access token for ``provider``.\n\nStores it in memory only. A malformed forward (empty token / non-positive\nexpiry) is rejected loudly (400) rather than deferred to an opaque 401 in a\nlater mailbox call.",
        "operationId": "import_forwarded_connection_v1_connections__provider__post",
        "parameters": [
          {
            "in": "path",
            "name": "provider",
            "required": true,
            "schema": {
              "title": "Provider",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ForwardedConnectionRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ForwardedConnectionSummary"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Import Forwarded Connection",
        "tags": [
          "email-forward-out"
        ]
      }
    },
    "/v1/email/archive": {
      "post": {
        "description": "Archive a message — gated on confirmation, reversible for 120s.\n\nThe gate fires FIRST: a request without a valid token for this exact\n``(action='archive', message_id)`` is rejected with 403 before any backend\ncall. On success a ``batch_id`` undo handle is returned; pass it to\nPOST /v1/email/unarchive within the window to restore the message. The\nresponse also surfaces ``post_archive_id`` (the id a folder backend mints on\nthe move) so a caller can track the message after Outlook changes its id.",
        "operationId": "archive_email_v1_email_archive_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EmailArchiveRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EmailArchiveResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "description": "Ambiguous or unknown account: the named provider is not connected, or no provider was given while several accounts are connected."
          },
          "403": {
            "description": "Authorization failed — the account's auth is missing, expired, or revoked (reconnect via Settings → Connectors), or a confirmation gate rejected the request (mint a fresh confirmation token)."
          },
          "409": {
            "description": "The archive did not take effect — the message is still in the inbox (the provider's modify call did not remove the INBOX label)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          },
          "502": {
            "description": "Upstream provider/connector failure."
          },
          "503": {
            "description": "No account connected, or backend configuration is missing — connect Google or Microsoft in Settings → Connectors."
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Archive Email",
        "tags": [
          "email"
        ]
      }
    },
    "/v1/email/attention": {
      "get": {
        "description": "The read-only \"what needs you\" attention view (#2582).\n\nMerges four signals by calling the underlying tools directly, never the\n``/prescan`` envelope (whose ``informational_count`` carries no rows, so\na meeting proposal in a confidently-classified informational message\nwould otherwise be invisible): inbound waiting-on-you items (#2581),\nmeeting proposals found during the scan (#2583), unreviewed messages\n(#2584), and open action items from prior triage (#2110/#2525).\n\nComputed on open and cached (no scheduler dependency, #2379/#2585): a\ncall within the freshness window returns the cached result with its real\n``cache_age_seconds``; past that window a fresh scan is attempted, and a\nfailed refresh falls back to the last known-good result marked\n``stale=True`` rather than presenting it as current. Read-only\nthroughout — this never archives, marks, replies, or sends.",
        "operationId": "get_attention_view_v1_email_attention_get",
        "parameters": [
          {
            "in": "query",
            "name": "max_messages",
            "required": false,
            "schema": {
              "default": 50,
              "title": "Max Messages",
              "type": "integer"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EmailAttentionResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "description": "Authorization failed — the account's auth is missing, expired, or revoked (reconnect via Settings → Connectors), or a confirmation gate rejected the request (mint a fresh confirmation token)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          },
          "502": {
            "description": "Upstream provider/connector failure."
          },
          "503": {
            "description": "No account connected, or backend configuration is missing — connect Google or Microsoft in Settings → Connectors."
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Get Attention View",
        "tags": [
          "email"
        ]
      }
    },
    "/v1/email/briefing": {
      "get": {
        "description": "Return the latest scheduled inbox briefing (#1608).\n\nThe briefing is generated without a user prompt by the sidecar's daily\nscheduler (off by default — ``GAIA_EMAIL_BRIEFING_ENABLED``) and\npersisted by ``gaia_agent_email.briefing``; this endpoint is the pull\nsurface any host reads it from. 404 until a scheduled run has happened.",
        "operationId": "get_briefing_v1_email_briefing_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EmailBriefingResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "404": {
            "description": "No briefing generated yet — enable the daily schedule (GAIA_EMAIL_BRIEFING_ENABLED) or POST /v1/email/prescan."
          },
          "500": {
            "description": "The persisted briefing could not be read or does not match the email_pre_scan envelope."
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Get Briefing",
        "tags": [
          "email"
        ]
      }
    },
    "/v1/email/calendar/events": {
      "get": {
        "description": "View calendar events on the primary calendar (read-only).\n\n``time_min`` / ``time_max`` are optional RFC 3339 bounds; when both are\nomitted the listing defaults to a forward window (now → +30 days), so\nrecurring series don't surface their oldest instances. ``provider``\n(google|microsoft) is required only when more than one account is connected;\nwith exactly one, it is inferred. Reaches whichever calendar the user\nconnected. Fails loudly (403 + reconnect CTA) if the calendar scope is missing.",
        "operationId": "list_calendar_events_v1_email_calendar_events_get",
        "parameters": [
          {
            "in": "query",
            "name": "time_min",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Time Min"
            }
          },
          {
            "in": "query",
            "name": "time_max",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Time Max"
            }
          },
          {
            "in": "query",
            "name": "provider",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Provider"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CalendarEventsResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "description": "Ambiguous or unknown account: the named provider is not connected, or no provider was given while several accounts are connected."
          },
          "403": {
            "description": "Authorization failed — the account's auth is missing, expired, or revoked (reconnect via Settings → Connectors), or a confirmation gate rejected the request (mint a fresh confirmation token)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          },
          "502": {
            "description": "Upstream provider/connector failure."
          },
          "503": {
            "description": "No account connected, or backend configuration is missing — connect Google or Microsoft in Settings → Connectors."
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "List Calendar Events",
        "tags": [
          "email"
        ]
      },
      "post": {
        "description": "Create a calendar event — gated on explicit confirmation (#1780).\n\nMirrors the send gate: a request without a valid, payload-bound confirmation\ntoken (from POST /calendar/events/preview) is rejected with HTTP 403 before\nany backend call. Creating an event is externally visible to attendees, so it\nis never performed without explicit confirmation.",
        "operationId": "create_calendar_event_v1_email_calendar_events_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CalendarCreateEventRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CalendarEventResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "description": "Bad event input (no usable time / inverted window), or the provider is ambiguous/not connected."
          },
          "403": {
            "description": "Authorization failed — the account's auth is missing, expired, or revoked (reconnect via Settings → Connectors), or a confirmation gate rejected the request (mint a fresh confirmation token)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          },
          "502": {
            "description": "Upstream provider/connector failure."
          },
          "503": {
            "description": "No account connected, or backend configuration is missing — connect Google or Microsoft in Settings → Connectors."
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Create Calendar Event",
        "tags": [
          "email"
        ]
      }
    },
    "/v1/email/calendar/events/preview": {
      "post": {
        "description": "Mint a single-use confirmation token bound to the proposed event.\n\nThe calendar analogue of POST /v1/email/draft: it creates nothing and reads\nno calendar — it only returns the normalized event plus a ``confirmation_token``\nthe caller echoes to POST /v1/email/calendar/events to authorize the create.",
        "operationId": "preview_calendar_event_v1_email_calendar_events_preview_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CalendarCreateEventRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CalendarEventPreviewResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Preview Calendar Event",
        "tags": [
          "email"
        ]
      }
    },
    "/v1/email/calendar/events/respond": {
      "post": {
        "description": "RSVP accept / decline / tentative to an existing calendar invite.\n\nAn explicit, user-initiated action (the UI's accept/decline controls), so it\nis not separately token-gated. ``attendee_email`` is the principal's own\naddress (used by the Google backend; ignored by Outlook, which RSVPs on /me).",
        "operationId": "respond_calendar_event_v1_email_calendar_events_respond_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/CalendarRespondRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CalendarRespondResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "description": "Ambiguous or unknown account: the named provider is not connected, or no provider was given while several accounts are connected."
          },
          "403": {
            "description": "Authorization failed — the account's auth is missing, expired, or revoked (reconnect via Settings → Connectors), or a confirmation gate rejected the request (mint a fresh confirmation token)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          },
          "502": {
            "description": "Upstream provider/connector failure."
          },
          "503": {
            "description": "No account connected, or backend configuration is missing — connect Google or Microsoft in Settings → Connectors."
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Respond Calendar Event",
        "tags": [
          "email"
        ]
      }
    },
    "/v1/email/confirm": {
      "post": {
        "description": "Mint a single-use confirmation token for a destructive mailbox action.\n\nThe token authorizes exactly one ``(action, message_id)`` — the archive or\nquarantine call must echo it back. Nothing mutates here; this is the\nexplicit user-confirmation step (the action analogue of POST /v1/email/draft\nfor sends). When ``provider`` is set the token carries that binding so the\naction routes to the right mailbox.",
        "operationId": "confirm_action_v1_email_confirm_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EmailActionConfirmRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EmailActionConfirmResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Confirm Action",
        "tags": [
          "email"
        ]
      }
    },
    "/v1/email/draft": {
      "post": {
        "description": "Propose a reply and mint a confirmation token bound to its payload.\n\nThe returned token must be echoed to :func:`send_email` to authorize\nsending exactly this ``(to, subject, body, attachments)``. This is the explicit\nuser-confirmation step — the consuming app surfaces the draft to the\nuser, and only a user-approved send echoes the token back.",
        "operationId": "draft_reply_v1_email_draft_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EmailDraftRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EmailDraftResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Draft Reply",
        "tags": [
          "email"
        ]
      }
    },
    "/v1/email/health": {
      "get": {
        "description": "Report that the email REST surface is mounted and serving.\n\nDependency-light: no live mailbox, no LLM. A host uses this for the sidecar\nreadiness handshake and for liveness checks once mounted on the product app.",
        "operationId": "email_health_v1_email_health_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HealthResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "security": [],
        "summary": "Email Health",
        "tags": [
          "email"
        ]
      }
    },
    "/v1/email/init": {
      "get": {
        "description": "Readiness preflight: validate the whole triage stack (#1795).\n\nReturns HTTP 200 when ready, 503 when not (with an actionable ``hint``).\nUnlike ``/health`` (liveness-only — never touches the LLM), this probes the\nlocal Lemonade Server, checks it is at a compatible VERSION (>= the agent's\n``MIN_LEMONADE_VERSION``), and confirms the triage model is downloaded — so a\nhost can verify \"ready to triage,\" not just \"process up.\" Read-only — no\nmodel pull or provisioning is triggered.",
        "operationId": "email_init_v1_email_init_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InitResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "503": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InitResponse"
                }
              }
            },
            "description": "Service Unavailable"
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Email Init",
        "tags": [
          "email"
        ]
      }
    },
    "/v1/email/prescan": {
      "post": {
        "description": "Pre-scan the connected inbox into the scannable triage card envelope.\n\nLists the ``max_messages`` most-recent inbox messages via every connected\nmailbox and returns the aggregate pre-scan summary the Agent UI's\n``EmailPreScanCard`` renders — top urgent / actionable rows, an\ninformational count, and suggested archives, each with a heuristic reason.\nRead-only: nothing is archived, marked, or sent.\n\nClassification reuses the agent's ``pre_scan_inbox_impl`` (the same\nheuristic path the agent loop runs) — categories are not re-implemented\nhere. The backend is resolved by :func:`get_prescan_backend`: 503 when no\nmailbox is connected, and with 2+ connected it consolidates every mailbox\ninto one merged envelope rather than failing on ambiguity (the frozen REST\ncontract carries no per-item mailbox tag).",
        "operationId": "prescan_inbox_v1_email_prescan_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EmailPreScanRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EmailPreScanResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "403": {
            "description": "Authorization failed — the account's auth is missing, expired, or revoked (reconnect via Settings → Connectors), or a confirmation gate rejected the request (mint a fresh confirmation token)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          },
          "502": {
            "description": "Upstream provider/connector failure."
          },
          "503": {
            "description": "No account connected, or backend configuration is missing — connect Google or Microsoft in Settings → Connectors."
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Prescan Inbox",
        "tags": [
          "email"
        ]
      }
    },
    "/v1/email/quarantine": {
      "post": {
        "description": "Quarantine a phishing message — gated on confirmation, reversible for 120s.\n\nApplies the ``GAIA_PHISHING_QUARANTINE`` label and removes the message from\nthe inbox (capability #9). The gate fires FIRST (403 without a valid token\nfor this ``(action='quarantine', message_id)``). The action refuses\n``is_phishing=False`` (400) — only phishing-flagged mail may be quarantined.\nReverse with POST /v1/email/unquarantine using the returned ``action_id``.",
        "operationId": "quarantine_email_v1_email_quarantine_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EmailQuarantineRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EmailQuarantineResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "description": "Refused: is_phishing was false (only phishing-flagged mail may be quarantined), the mailbox is Outlook (quarantine is Gmail-only), or the provider is ambiguous/not connected."
          },
          "403": {
            "description": "Authorization failed — the account's auth is missing, expired, or revoked (reconnect via Settings → Connectors), or a confirmation gate rejected the request (mint a fresh confirmation token)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          },
          "502": {
            "description": "Upstream provider/connector failure."
          },
          "503": {
            "description": "No account connected, or backend configuration is missing — connect Google or Microsoft in Settings → Connectors."
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Quarantine Email",
        "tags": [
          "email"
        ]
      }
    },
    "/v1/email/query": {
      "post": {
        "description": "Run the email agent loop for one request and stream canonical SSE events.\n\nBuilds an agent, injects the pushed ``context`` as conversation history, runs\n``process_query(query)`` on a worker thread with an ``SSEOutputHandler``, and\nrelays the loop as the seven canonical event types (spec §4). The stream ends\nwith exactly one ``final`` or ``error``. A confirmation-requiring step ends the\nstream with a ``needs_confirmation`` followed by a ``final`` refusal (the\nstateless D1 stub — see module docstring). A step that asks the user a\nquestion emits ``needs_input`` and the stream STAYS OPEN, resuming once\n``POST /v1/email/query/{run_id}/respond`` delivers the answer.",
        "operationId": "query_v1_email_query_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/QueryRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              },
              "text/event-stream": {
                "schema": {
                  "example": "data: {\"type\": \"status\", \"message\": \"Processing...\"}\n\ndata: {\"type\": \"tool_call\", \"tool\": \"triage_inbox\", \"args\": {}}\n\ndata: {\"type\": \"tool_result\", \"tool\": \"triage_inbox\", \"data\": {}}\n\ndata: {\"type\": \"final\", \"answer\": \"Triaged 5 emails.\"}\n\n",
                  "type": "string"
                }
              }
            },
            "description": "Server-Sent Events stream (text/event-stream). Each `data:` line is one canonical event discriminated on `type`, one of: status {message} | token {delta} | tool_call {tool, args} | tool_result {tool, render?, data} | needs_confirmation {run_id, action, summary} | needs_input {run_id, request_id, question, options, allow_free_text, respond_url, timeout_seconds?} | final {answer, usage?} | error {detail, status}. The stream is terminated by exactly one `final` or one `error`; `needs_input` pauses the run until POST /v1/email/query/{run_id}/respond delivers the answer, then the SAME stream continues. `:`-prefixed heartbeat comments may appear at any time and carry no payload."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Query",
        "tags": [
          "email",
          "email-query"
        ]
      }
    },
    "/v1/email/query/{run_id}/cancel": {
      "post": {
        "description": "Cancel an in-flight ``/query`` run — stops tool execution between steps.\n\nCooperative, not a kill: it sets the run's cancel flag, which the agent loop\nobserves at its next step boundary (per-tool timeouts keep each step bounded,\nso that point is always reached in finite time) and the handler observes while\nwaiting on any confirmation. 404 if no run with that id is in flight.",
        "operationId": "cancel_query_v1_email_query__run_id__cancel_post",
        "parameters": [
          {
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "title": "Run Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/QueryCancelResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Cancel Query",
        "tags": [
          "email",
          "email-query"
        ]
      }
    },
    "/v1/email/query/{run_id}/respond": {
      "post": {
        "description": "Answer a ``needs_input`` question and let the paused run continue.\n\nThe run's worker thread is blocked inside ``request_user_input_blocking``;\nthis hands it the answer through the same out-of-band resolve the stateful\nAgent UI path uses, and the run resumes emitting on its ORIGINAL ``/query``\nstream — no second request, no replayed context.\n\nFailure modes are distinct on purpose, because \"the agent never saw my\nanswer\" is otherwise indistinguishable from \"the agent is thinking\":\n\n- **404** — no run with that ``run_id`` is in flight (it finished, was\n  cancelled, or the id is wrong).\n- **409** — the run is live but ``request_id`` is not what it is waiting on:\n  a stale answer to a question that already timed out or was answered.\n  Rejected rather than applied to whatever question is pending now.",
        "operationId": "respond_query_v1_email_query__run_id__respond_post",
        "parameters": [
          {
            "in": "path",
            "name": "run_id",
            "required": true,
            "schema": {
              "title": "Run Id",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/QueryRespondRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/QueryRespondResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Respond Query",
        "tags": [
          "email",
          "email-query"
        ]
      }
    },
    "/v1/email/search": {
      "post": {
        "description": "Search the connected mailbox (read-only) — #1781.\n\nLists messages matching ``query``/``labels`` from the connected Gmail or\nOutlook mailbox and returns inbox-list metadata (id, thread, subject, from,\nto, date, snippet, labels) — not the message body. No mail is sent or\nmodified. This restores the agent's in-loop inbox-search capability on the\nREST contract so the Agent UI can drive it through the package.\n\nThe mailbox is resolved by :func:`get_search_backend` (fail-loud on 0 or\nambiguous mailbox counts). Backend auth / config / transport errors surface\nas actionable 4xx/5xx, never a silent empty result.",
        "operationId": "search_inbox_v1_email_search_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EmailSearchRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EmailSearchResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "description": "Ambiguous or unknown account: the named provider is not connected, or no provider was given while several accounts are connected. Also returned when ``query`` carries an unparseable date or duration operator value (e.g. ``newer_than:1.5w``)."
          },
          "403": {
            "description": "Authorization failed — the account's auth is missing, expired, or revoked (reconnect via Settings → Connectors), or a confirmation gate rejected the request (mint a fresh confirmation token)."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          },
          "502": {
            "description": "Upstream provider/connector failure."
          },
          "503": {
            "description": "No account connected, or backend configuration is missing — connect Google or Microsoft in Settings → Connectors."
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Search Inbox",
        "tags": [
          "email"
        ]
      }
    },
    "/v1/email/send": {
      "post": {
        "description": "Send a reply — gated on explicit confirmation (#1264).\n\nThe confirmation gate is enforced FIRST: a request without a valid,\npayload-bound confirmation token is rejected with HTTP 403 before any\nbackend call (or even backend resolution). This mirrors the agent's\n``CONFIRMATION_REQUIRED_TOOLS`` guard, translated to the API boundary,\nand guarantees the gate fires regardless of backend health. Never\nauto-confirms.",
        "operationId": "send_email_v1_email_send_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EmailSendRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EmailSendResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "description": "Ambiguous or unknown account: the named provider is not connected, or no provider was given while several accounts are connected."
          },
          "403": {
            "description": "Authorization failed — the account's auth is missing, expired, or revoked (reconnect via Settings → Connectors), or a confirmation gate rejected the request (mint a fresh confirmation token)."
          },
          "413": {
            "description": "An attachment exceeds the connected provider's size ceiling."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          },
          "502": {
            "description": "Upstream provider/connector failure."
          },
          "503": {
            "description": "No account connected, or backend configuration is missing — connect Google or Microsoft in Settings → Connectors."
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Send Email",
        "tags": [
          "email"
        ]
      }
    },
    "/v1/email/triage": {
      "post": {
        "description": "Triage a single email or a full thread.\n\nAccepts the FROZEN #1262 ``EmailTriageRequest`` (a single-email or\nthread payload, discriminated on ``kind``) and returns the structured\n``EmailTriageResponse`` — category, spam/phishing signals, a plain-text\nsummary, extracted action items, and an optional draft-reply proposal.\nNo mail is read or sent; this analyzes only the payload in the request.\nExtracted action items are also persisted to the local task store,\nlinked to the source ``message_id`` and de-duplicated per message\n(#1605) — additive, the response shape is unchanged.",
        "operationId": "triage_email_v1_email_triage_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EmailTriageRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EmailTriageResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          },
          "502": {
            "description": "Local LLM triage failed — Lemonade Server unreachable or it returned an unusable result."
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Triage Email",
        "tags": [
          "email"
        ]
      }
    },
    "/v1/email/triage/batch": {
      "post": {
        "description": "Triage an array of emails or threads in a single request (#1887).\n\nAccepts a ``BatchTriageRequest`` (1..MAX_BATCH_SIZE ``EmailInput`` items)\nand returns a ``BatchTriageResponse`` — one ``BatchItemResult`` per item,\norder-preserved. Per-item failures surface as ``BatchItemResult.error``\n(HTTP 200 with all items errored is valid — inspect each result). A 502\nmeans Lemonade was unreachable or the triage model is unavailable there,\ndetected before any item was processed. No mail is read or sent; this\nanalyzes only the items in the request. Each\nsuccessful item's action items are persisted to the local task store,\nlinked to its source ``message_id`` and de-duplicated per message\n(#1605) — additive, the response shape is unchanged.",
        "operationId": "triage_email_batch_v1_email_triage_batch_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/BatchTriageRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BatchTriageResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          },
          "502": {
            "description": "Local LLM triage failed — Lemonade Server unreachable or it returned an unusable result."
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Triage Email Batch",
        "tags": [
          "email"
        ]
      }
    },
    "/v1/email/unarchive": {
      "post": {
        "description": "Reverse an archive within the undo window (NOT gated — it restores).\n\nRoutes each row to the mailbox it was archived from and uses the recorded\n``post_archive_id`` so a folder-based backend (Outlook) can find the message\nafter its id changed (#1738). Fails loudly (409) when the window has expired\nor the ``batch_id`` has no undoable rows — never a silent no-op.",
        "operationId": "unarchive_email_v1_email_unarchive_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EmailUnarchiveRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EmailUnarchiveResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "description": "Ambiguous or unknown account: the named provider is not connected, or no provider was given while several accounts are connected."
          },
          "403": {
            "description": "Authorization failed — the account's auth is missing, expired, or revoked (reconnect via Settings → Connectors), or a confirmation gate rejected the request (mint a fresh confirmation token)."
          },
          "409": {
            "description": "The undo window has expired, or the handle is unknown or already undone — the action can no longer be reversed via the API."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          },
          "502": {
            "description": "Upstream provider/connector failure."
          },
          "503": {
            "description": "No account connected, or backend configuration is missing — connect Google or Microsoft in Settings → Connectors."
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Unarchive Email",
        "tags": [
          "email"
        ]
      }
    },
    "/v1/email/unquarantine": {
      "post": {
        "description": "Reverse a quarantine within the undo window (NOT gated — it restores).\n\nRestores the exact label set recorded at quarantine time and removes the\nquarantine label. Fails loudly (409) when the window has expired or the\n``action_id`` is unknown/already undone — never a silent no-op.",
        "operationId": "unquarantine_email_v1_email_unquarantine_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/EmailUnquarantineRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/EmailUnquarantineResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "400": {
            "description": "Refused: the recorded mailbox is Outlook (quarantine is Gmail-only), or the provider is ambiguous/not connected."
          },
          "403": {
            "description": "Authorization failed — the account's auth is missing, expired, or revoked (reconnect via Settings → Connectors), or a confirmation gate rejected the request (mint a fresh confirmation token)."
          },
          "409": {
            "description": "The undo window has expired, or the handle is unknown or already undone — the action can no longer be reversed via the API."
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          },
          "502": {
            "description": "Upstream provider/connector failure."
          },
          "503": {
            "description": "No account connected, or backend configuration is missing — connect Google or Microsoft in Settings → Connectors."
          }
        },
        "security": [
          {
            "bearerAuth": []
          },
          {}
        ],
        "summary": "Unquarantine Email",
        "tags": [
          "email"
        ]
      }
    },
    "/v1/email/version": {
      "get": {
        "description": "Report the REST/contract version and the package build version.\n\nBoth values come from ``gaia_agent_email.version`` — the same constants the\nfreeze server's root ``/version`` reads — so the product surface and the\nfrozen binary can never disagree on what contract they speak.",
        "operationId": "email_version_v1_email_version_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/VersionResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "security": [],
        "summary": "Email Version",
        "tags": [
          "email"
        ]
      }
    }
  }
}
