# Copyright(C) 2025-2026 Advanced Micro Devices, Inc. All rights reserved.
# SPDX-License-Identifier: MIT

# Python compiled files
*.pyc
*.pyo
*.pyd
*.bak
**/__pycache__/

# Python distribution / packaging
.Python
env/
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
output/
*.egg-info/
.installed.cfg
*.egg

# PyInstaller
#  Usually these files are written by a python script from a template
#  before PyInstaller builds the exe, so as to inject date/other infos into it.
*.manifest
*.spec

# Installer logs
pip-log.txt
pip-delete-this-directory.txt

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/

# Translations
*.mo
*.pot

# Django stuff
*.log
*.pid
local_settings.py

# Flask stuff
instance/
.webassets-cache

# Scrapy stuff
.scrapy

# Sphinx documentation
docs/_build/

# PyBuilder
target/

# Jupyter Notebook
.ipynb_checkpoints

# pyenv
.python-version

# celery beat schedule file
celerybeat-schedule

# SageMath parsed files
*.sage.py

# dotenv
.env

# virtualenv
.venv
venv/
gaiaenv/
ENV/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/

# IDE settings
.idea/

# Claude Code local settings
.claude/settings.local.json
.claude/skill-meta/
.claude/plans/

# Local customizations
util/custom/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Node.js
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.npm
.yarn-integrity
.pnp
.pnp.js
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# Lock files (uncomment if you want to ignore them)
# package-lock.json
# yarn.lock
# pnpm-lock.yaml

# Node.js build outputs
build/
# Allow committed demo agents in the installer staging dir
!src/gaia/apps/webui/build/
!src/gaia/apps/webui/build/bundled-agents/
!src/gaia/apps/webui/build/bundled-agents/**
src/gaia/apps/webui/build/bundled-agents/**/__pycache__/
src/gaia/apps/webui/build/bundled-agents/**/*.pyc
dist/
out/
.next/
.nuxt/
.cache/
.parcel-cache/

# Node.js coverage and testing
coverage/
.nyc_output
*.lcov

# Node.js environment files
.env.local
.env.development.local
.env.test.local
.env.production.local

# Node.js package files
*.tgz
*.tar.gz

# Node.js cache files
.eslintcache
.stylelintcache

# Custom
config.ini

# GAIA cache directory
.gaia/
.pypirc

# Electron Apps - exclude .env but keep .env.development
src/gaia/apps/**/.env
!src/gaia/apps/**/.env.development

# Claude Code local settings (personal, not committed)
.claude/settings.local.json

# MCP Configuration and runtime files
.env
.env.*
!.env.example
mcp_server.log
atlassian_config.json
*.mcp.json
.mcp.json
!.devcontainer/.mcp.json

# Local settings files (machine-specific, not for repo)
*local.json

# version files
version.txt
installer/version.nsh

# Installer files
installer/**.exe
release_info.json

# Lemonade MSI downloaded at build time (issue #774)
installer/lemonade-server-minimal.msi

# version
version.txt
**/version.nsh
**/.gaia_servers.json
**/agent_output_*.json

# vcpkg manifest-mode installed packages
cpp/vcpkg_installed/

# CMake build artifacts (generated files, not source)
CMakeFiles/
cpp/build_ssl/

# C++ benchmark output (ephemeral — stored in CI cache/artifacts, not source control)
cpp/benchmark-*.json

# SD test results and artifacts
sd_model_sweep_results/
quick_test_results/
test_*.png
diffusers_*.png
sdxl_base_*.png
docs/playbooks/sd-agent/index-backup.mdx

# Claude custom settings
.claude/settings.local.json

# Custom util scripts
util/custom/*

# Real-world eval corpus — synthetic docs are now generated by
# eval/corpus/gen_real_world.py and committed; ignore only ad-hoc scratch
# files inside the directory if any are introduced later.

# Local-only eval scenarios + their tests — vendor-coupled tool-call
# probes that depend on a specific MCP service being installed locally.
# Kept on disk so internal evals continue to run; not shipped to the
# public repo because the scenarios reference tool names that only
# exist in that specific MCP integration.
eval/scenarios/mcp_tool_reliability/
tests/unit/eval/test_mcp_tool_reliability_scenarios.py

# Generated slide decks from the gaia-{executive,technical}-presentation skills.
# Repo-root only — the leading slash deliberately spares the committed
# docs/presentations/ decks.
/presentations/
# Internal skill-marketplace installs (slai-marketplace). AMD-internal skills
# copied into per-tool dirs; never ship them to the public repo. The repo's own
# committed skills under .claude/skills/* stay tracked — only marketplace-installed
# skill dirs (e.g. m365-*) are ignored. skills/community/ is the curated contribution
# lane and stays tracked. The shipped starter pack (#893) now lives in the hub's skills
# lane at hub/skills/, which this rule never covered.
/skills/*
!/skills/community/
/.cline/
/.codex/
/.cursor/
/.claude/skills/m365-email/
# Claudia agent worktrees (local orchestration scratch)
.claudia-worktrees/

# Private working reports — analysis, competitive research, benchmark logs.
# These are internal notes, not project documentation, and several were
# published to a public fork and a public PR before this rule existed. They
# live outside the repo now (../gaia-private-reports/). Project docs belong in
# docs/ and are reviewed like any other change; anything matching these
# patterns is assumed to be private working material.
/BENCHMARK_ANALYSIS.md
/GAIA_AGENT_V2_SCOPE.md
/GAIA_CODEBASE_DEEPDIVE.md
/GAIA_COMPETITIVE_ANALYSIS.md
/GAIA_COMPETITIVE_GAP_ANALYSIS_HERMES_OPENCLAW.md
/GAIA_MEASURED_BENCHMARKS.md
/TESTING_STATUS.md
/*_ANALYSIS.md
/*_DEEPDIVE.md
/GAIA_*_REPORT.md

# Per-run gaia eval fixture hub (built by tests/fixtures/gaia/prepare_fixture_hub.py;
# signed with an ephemeral key that must never be committed)
tests/fixtures/gaia/fixture_hub/_prepared/
eval/results/gaia-local-tui-validation/tui-home/
