# Copyright(C) 2025-2026 Advanced Micro Devices, Inc. All rights reserved.
# SPDX-License-Identifier: MIT

# This workflow tests the GAIA Chat Agent, which ships as the standalone
# gaia-agent-chat wheel (#1102). Tests include the wheel's own smoke tests
# plus the framework-side session, RAG, and path-validation suites.

name: Chat Agent Tests

on:
  workflow_call:
  push:
    branches: [ main ]
    paths:
      - 'hub/agents/chat/python/**'
      - 'src/gaia/agents/base/**'
      - 'src/gaia/agents/tools/**'
      - 'src/gaia/web/**'
      - 'src/gaia/rag/**'
      - 'src/gaia/chat/**'
      - 'tests/unit/test_browser_tools.py'
      - 'tests/unit/test_file_tools.py'
      - 'tests/unit/test_filesystem_tools_mixin.py'
      - 'tests/unit/test_chat_tool_bundles.py'
      - 'tests/unit/api/test_sse_confirmation_gate.py'
      - 'tests/unit/agents/test_output_artifacts.py'
      - 'tests/unit/agents/test_large_tool_result_truncation.py'
      - 'tests/unit/agents/test_parallel_tool_calls.py'
      - 'tests/test_chat_agent.py'
      - 'tests/test_chat_sdk.py'
      - 'setup.py'
      - '.github/workflows/test_chat_agent.yml'
  pull_request:
    # No `branches:` filter: it matches the PR base, so `[ main ]` skipped stacked PRs (#2767).
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - 'hub/agents/chat/python/**'
      - 'src/gaia/agents/base/**'
      - 'src/gaia/agents/tools/**'
      - 'src/gaia/web/**'
      - 'src/gaia/rag/**'
      - 'src/gaia/chat/**'
      - 'tests/unit/test_browser_tools.py'
      - 'tests/unit/test_file_tools.py'
      - 'tests/unit/test_filesystem_tools_mixin.py'
      - 'tests/unit/test_chat_tool_bundles.py'
      - 'tests/unit/api/test_sse_confirmation_gate.py'
      - 'tests/unit/agents/test_output_artifacts.py'
      - 'tests/unit/agents/test_large_tool_result_truncation.py'
      - 'tests/unit/agents/test_parallel_tool_calls.py'
      - 'tests/test_chat_agent.py'
      - 'tests/test_chat_sdk.py'
      - 'setup.py'
      - '.github/workflows/test_chat_agent.yml'
  merge_group:
  workflow_dispatch:

# Cancel in-progress runs when a new run is triggered
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  test-chat-agent:
    name: Test Chat Agent
    runs-on: ubuntu-latest
    if: github.event_name != 'pull_request' || github.event.pull_request.draft == false || contains(github.event.pull_request.labels.*.name, 'ready_for_ci')

    steps:
      - uses: actions/checkout@v7

      - name: Free disk space
        uses: ./.github/actions/free-disk-space

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.12'

      - name: Install uv
        run: curl -LsSf https://astral.sh/uv/install.sh | sh

      - name: Install dependencies
        run: |
          uv pip install --system -e .[dev,rag,api]
          # Install pytest-mock for mocking tests
          uv pip install --system pytest-mock
          # ChatAgent ships as the standalone gaia-agent-chat wheel (#1102)
          uv pip install --system -e hub/agents/chat/python

      - name: Test bounded tool output
        env:
          GAIA_MEMORY_DISABLED: "1"
        run: |
          python -m pytest tests/unit/agents/test_output_artifacts.py \
            tests/unit/agents/test_large_tool_result_truncation.py \
            tests/unit/agents/test_parallel_tool_calls.py tests/unit/test_browser_tools.py \
            tests/unit/test_file_tools.py tests/unit/test_filesystem_tools_mixin.py \
            tests/unit/test_chat_tool_bundles.py tests/unit/api/test_sse_confirmation_gate.py -v --tb=short

      - name: Run Chat Agent Package Tests
        env:
          GAIA_MEMORY_DISABLED: "1"
        run: |
          echo "================================================================"
          echo "                  CHAT AGENT PACKAGE TESTS"
          echo "================================================================"
          echo "Testing registration shapes, lazy re-exports, and discovery..."
          echo ""
          python -m pytest hub/agents/chat/python/tests/ -v --tb=short

      - name: Run Framework Registry Discovery Tests
        env:
          GAIA_MEMORY_DISABLED: "1"
        run: |
          echo "================================================================"
          echo "           FRAMEWORK REGISTRY DISCOVERY TESTS"
          echo "================================================================"
          # tests/unit/agents/test_registry.py::TestBuiltinRegistration has
          # chat-specific cases that importorskip("gaia_agent_chat") -- this
          # is the only CI job that installs that wheel, so it's the only
          # place these actually run instead of silently skipping (#2240).
          echo "Verifying AgentRegistry actually resolves the installed gaia-agent-chat wheel..."
          echo ""
          python -m pytest tests/unit/agents/test_registry.py -v --tb=short

      - name: Run Chat Agent Unit Tests
        env:
          # Skip memory init — Lemonade isn't available in this CI job; the
          # ChatAgent unit tests don't exercise memory functionality.
          GAIA_MEMORY_DISABLED: "1"
        run: |
          echo "================================================================"
          echo "                    CHAT AGENT UNIT TESTS"
          echo "================================================================"
          echo "Testing core functionality: initialization, tools, session management..."
          echo ""

          # Run unit tests
          python -m pytest tests/test_chat_agent.py::TestChatAgent -v --tb=short

          # Store the result
          UNIT_TEST_EXIT=$?

          if [ $UNIT_TEST_EXIT -eq 0 ]; then
            echo "[SUCCESS] Unit tests passed"
          else
            echo "[FAILURE] Unit tests failed"
            exit 1
          fi

      - name: Run Chat Agent Session Tests
        env:
          GAIA_MEMORY_DISABLED: "1"
        run: |
          echo ""
          echo "================================================================"
          echo "                 CHAT AGENT SESSION TESTS"
          echo "================================================================"
          echo "Testing session persistence and chat history..."
          echo ""

          # Run session tests
          python -m pytest tests/test_chat_agent.py::TestChatAgentSessions -v --tb=short

          # Store the result
          SESSION_TEST_EXIT=$?

          if [ $SESSION_TEST_EXIT -eq 0 ]; then
            echo "[SUCCESS] Session tests passed"
          else
            echo "[FAILURE] Session tests failed"
            exit 1
          fi

      - name: Run Chat Agent Path Validation Tests
        env:
          GAIA_MEMORY_DISABLED: "1"
        run: |
          echo ""
          echo "================================================================"
          echo "              CHAT AGENT PATH VALIDATION TESTS"
          echo "================================================================"
          echo "Testing path security and validation..."
          echo ""

          # Run path validation tests
          python -m pytest tests/test_chat_agent.py::TestChatAgentPathValidation -v --tb=short

          # Store the result
          PATH_TEST_EXIT=$?

          if [ $PATH_TEST_EXIT -eq 0 ]; then
            echo "[SUCCESS] Path validation tests passed"
          else
            echo "[FAILURE] Path validation tests failed"
            exit 1
          fi

      - name: Run Chat Agent Host-Attribute Contract Tests
        env:
          GAIA_MEMORY_DISABLED: "1"
        run: |
          echo ""
          echo "================================================================"
          echo "         CHAT AGENT HOST-ATTRIBUTE CONTRACT TESTS"
          echo "================================================================"
          echo "Testing ChatAgent/ChatAgentLite are unaffected by the"
          echo "RAGToolsMixin/FileIOToolsMixin host-attribute contract fix..."
          echo ""

          python -m pytest tests/test_chat_agent.py::TestChatAgentHostAttributeContract -v --tb=short

          # Store the result
          CONTRACT_TEST_EXIT=$?

          if [ $CONTRACT_TEST_EXIT -eq 0 ]; then
            echo "[SUCCESS] Host-attribute contract tests passed"
          else
            echo "[FAILURE] Host-attribute contract tests failed"
            exit 1
          fi

      - name: Test Summary
        if: always()
        run: |
          echo ""
          echo "================================================================"
          echo "                   CHAT AGENT TEST SUMMARY"
          echo "================================================================"
          echo "Test Categories:"
          echo "  - Unit Tests: Agent initialization, tool registration"
          echo "  - Session Tests: Save/load sessions, chat history persistence"
          echo "  - Path Tests: Security validation, allowed paths"
          echo ""
          echo "Test Coverage:"
          echo "  - Chat agent initialization and configuration"
          echo "  - Session management (save/load/resume)"
          echo "  - Chat history persistence across sessions"
          echo "  - RAG document indexing"
          echo "  - Path validation and security"
          echo "================================================================"
