# Copyright(C) 2025-2026 Advanced Micro Devices, Inc. All rights reserved.
# SPDX-License-Identifier: MIT

# Agent eval gate for the Gemma-4-E4B consolidation.
#
# Every agent is moving onto `Gemma-4-E4B-it-GGUF`; agents that previously left
# `model_id` unset fell back to `Qwen3.5-35B-A3B-GGUF`. That is an LLM-affecting
# change, and CLAUDE.md requires `gaia eval agent` runs against the committed
# baselines before such a change lands. Unit tests cover code paths, not model
# behavior — this job is the only thing that catches a behavioral regression.
#
# Runner-only by construction. `gaia eval agent` drives the real GAIA UI backend
# against a real Lemonade Server on AMD hardware; a GitHub-hosted runner has
# neither. Hence the self-hosted pool, and the same shared `lemonade-eval`
# concurrency group as email_scorecard_refresh.yml / test_email_agent_eval.yml so
# this queues BEHIND them instead of race-evicting their loaded model (CLAUDE.md:
# at most one `gaia eval agent` process at a time, period).
#
# THIS POOL IS WINDOWS. Every inline `run:` step below is PowerShell, and Python
# setup goes through ./.github/actions/setup-venv (uv, Windows-aware) — the
# lab-runner convention shared by test_email_agent_eval.yml,
# email_scorecard_refresh.yml and test_agent_behavior_e2e.yml. Do NOT reintroduce
# bash here: this workflow's first-ever execution died instantly on
# `source .venv/bin/activate` because GitHub ran it through powershell.EXE, and
# every other bash step in the job hit the same parser. Windows also breaks the
# text-processing assumptions bash invites — the eval prints an `Output:` line
# containing a backslash path, which bash test/`[ -f ]` cannot consume.
#
# WHY THIS REPORTS BY DEFAULT INSTEAD OF HARD-FAILING (`enforce`, default false)
# ---------------------------------------------------------------------------
# `gaia eval agent --compare BASELINE CURRENT` already exits 2 on any status /
# score / time regression and 0 otherwise (src/gaia/cli.py). Wiring that exit
# code straight to the step result would make this job PERMANENTLY RED, because
# some regressions here are EXPECTED and accepted: code-generation and
# data-analysis scenarios are dropping from a 35B MoE to a ~4B dense model. A
# gate that is always red gates nothing — people stop reading it.
#
# So the default posture is REPORT: every category is compared, the full diff is
# printed, the scorecards are uploaded as an artifact, and a regression surfaces
# as a `::warning::` annotation. Make it blocking either by dispatching with
# `enforce: true`, or — since workflow_dispatch is only offered for workflows
# already on the default branch — by putting `[eval-enforce]` in the PR TITLE,
# which works on a branch before this file has merged.
#
# This mirrors how intentional regressions are handled elsewhere: the
# `--allow-regression` flag in src/gaia/eval/scorecard_gate.py prints a
# `::warning::` and returns 0 rather than pretending the regression did not
# happen, and test_email_agent_eval.yml ships its gates in report mode until the
# bars are trustworthy. Same idea, inverted default: report unless asked to
# enforce.
#
# REPORT MODE COVERS "WORSE" AND "DIDN'T MEASURE" ALIKE — VISIBLY. `--compare`
# scores only scenarios present on BOTH sides: scenarios that vanish from the
# current run land in an `only_in_baseline` bucket that is NOT part of its exit-2
# verdict (runner.py::compare_scorecards). Verified: dropping 6 of 7 rag_quality
# scenarios still exits 0. So a broken harness would otherwise sail through as "no
# regression". The integrity gate below is what refuses that: it fails if a
# baseline scenario is missing or any scenario produced no measurement
# (infra_error / errored / timeout / blocked / budget_exceeded / skipped).
#
# That gate used to ignore `enforce` and fail unconditionally, on the reasoning
# that report mode may soften "worse" but never "did not measure". The reasoning
# is sound and the outcome was not: the embedder cannot load on this runner
# (#3016), which is an environment defect no PR can fix, so the gate was red on
# every PR touching the path filter — including seven whose diffs never went near
# the eval — while this same job printed "report mode ... build stays green". A
# check that is always red gates nothing, and one that advertises non-blocking
# while blocking is worse than either honest posture.
#
# So `enforce` now decides the EXIT CODE for an incomplete run as well as a worse
# one. It never decides VISIBILITY: in report mode every finding is still a
# `::warning::` annotation AND a block in the job summary, so an incomplete run
# cannot be mistaken for a complete one. `enforce: true` restores blocking for
# both.
#
# ONE EXCEPTION, AND IT IS ABOUT BLAME, NOT CONVENIENCE: when the RAG embedder
# will not load on the runner at all (#3016, EMBEDDER_OK=false), rag_quality and
# context_retention warn instead of failing. Every other non-measurement says
# something about the PR under test; #3016 says only that this SoC cannot serve
# the embedder for ANY commit, so failing on it made the gate permanently red
# and unread. Nothing else is softened — tool_selection's own non-measurements
# still fail. Remove the exception when #3016 closes. See the gate step below.
#
# TIME REGRESSIONS ARE NOT COMPARABLE TO THIS BASELINE. `--compare` folds a >2x
# per-scenario wall-clock delta into the same exit 2 as a quality regression, but
# the committed baseline was captured against a REMOTE Lemonade over ngrok
# (see meta.json) on different hardware and an older Lemonade. Treat a time-only
# regression here as a hardware artifact, not a defect, until the baselines are
# regenerated on the runner itself.
#
# SEQUENCING. `--compare` only DIFFS two scorecards — it does NOT run an eval.
# So each category is evaluated first, the ABSOLUTE run directory is parsed from
# the eval's own `Output: <run-dir>` line, and `<run-dir>/scorecard.json` is then
# compared against the committed Gemma baseline. Never glob by mtime — a fresh
# clone stamps every file with the checkout time.
#
# PREREQUISITES ON THE RUNNER. Lemonade is NOT assumed to be already running:
# install-lemonade pins the runner to the expected LEMONADE_VERSION and
# ensure-lemonade-running.ps1 starts it as a persistent Scheduled Task and warms
# the model — exactly what test_email_agent_eval.yml and
# email_scorecard_refresh.yml do. (An earlier revision of this file claimed those
# two workflows assume a persistent server and start nothing; they do not, and
# that claim is what left this job with no way to bring Lemonade up.) The GAIA UI
# backend is separate and NOT persistent, so this workflow starts it itself — in
# the SAME shell session as the evals, because a backgrounded child does not
# survive a GitHub Actions step boundary (see the single-session note in
# test_agent_behavior_e2e.yml).
#
# Lemonade must serve TWO models, not one. ensure-lemonade-running.ps1 warms
# only the chat model, but rag_quality and context_retention answer out of RAG
# chunks, so the embedder is just as load-bearing — and when it cannot start,
# those categories collapse to ~2/10 non-answers that read as a model
# regression. The preflight therefore re-registers, re-pulls and then makes a
# real /embeddings call through tests/ci_lemonade_check.py — repairing the one
# cause CI can repair (a stale registration missing the embeddings label) and
# failing in seconds on the ones it cannot, instead of spending three and a half
# hours measuring nothing.
#
# The third prerequisite is the Claude Code CLI, which is easy to miss because
# ANTHROPIC_API_KEY looks like the whole story: `gaia eval agent` DRIVES each
# scenario by shelling out to `claude -p` with an MCP config, and its preflight
# refuses to start without it on PATH. Installed below via npm, as
# docs/guides/eval-ci.mdx prescribes.
#
# WHAT TRIGGERS IT. A `pull_request` path filter, so the gate fires on the PR's
# own base..head diff and only when that diff touches an LLM-affecting path. It
# used to be a `push` filter, which matches the files in the PUSH — so merging
# main into a branch replayed all of main's churn through the filter and fired
# this multi-hour gate on PRs containing no Python at all (#2807 and #2809 are
# C++-only and both wore its red X). Scoping is the only thing that changed: the
# path list is untouched, and a PR that does touch one still runs and still
# fails loudly.
#
# Two consequences of leaving `push` behind, both intended. A branch with no PR
# no longer triggers the gate — use `workflow_dispatch --ref <branch>`. And the
# checkout is now `refs/pull/N/merge`, so the eval measures main+PR rather than
# the branch head, which is the thing about to land.
#
# FORKS AND DRAFTS. Both are skipped by the job-level `if` below, not silently.
# A fork PR could not pass anyway — ANTHROPIC_API_KEY, the eval judge, is not
# exposed to fork PRs — and must not put unreviewed code on the lab pool. For
# the PRs that do run the key is present; if it ever is not, the preflight fails
# with instructions instead of running a judge-less eval that would produce a
# meaningless scorecard.

name: Agent Eval — Gemma-4-E4B consolidation

on:
  workflow_dispatch:
    inputs:
      enforce:
        description: 'Fail the build on a regression (false = report as ::warning:: only)'
        required: false
        type: boolean
        default: false
      eval_model:
        description: 'Driver + judge model for the eval harness (must match the baseline)'
        required: false
        default: 'claude-sonnet-4-6'
      deep_diagnostics:
        description: 'Run the full embedder-failure ladder (purges the model from disk, unloads all models). Off on PRs - it costs ~7 extra min and disturbs other jobs on the box.'
        required: false
        type: boolean
        default: false
      task_suite:
        description: 'Flagship task suite from eval/tasks/tasks.json (core runs on PRs; full is the deeper set)'
        required: false
        default: 'core'
  # `pull_request`, NOT `push`. A `push` path filter matches the files in the
  # PUSH, so merging main into a branch replays all of main's churn through the
  # filter — which is how #2807 and #2809, both pure C++ PRs with no Python in
  # their diff at all, ended up wearing a red X from an LLM-quality gate and
  # burning hours of the scarcest runner pool. `pull_request` filters on the
  # PR's own base..head diff, so the gate fires only when the PR itself edits a
  # path below. The `types:` list follows test_email_agent_eval.yml;
  # `ready_for_review` is what runs the gate on a PR
  # that was opened as a draft (see the draft clause in the job `if:`).
  #
  # THE PATH LIST BELOW IS UNCHANGED ON PURPOSE. This change is mechanism only:
  # it makes the filter mean what it already says. Note the list now includes
  # `hub/agents/*/python/**`, so 18 hub packages newly qualify for a
  # single-slot pool — scoping to the PR diff is what keeps that affordable.
  pull_request:
    # No `branches:` filter: it matches the PR base, so `[ main ]` skipped stacked PRs (#2767).
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      # The consolidation itself.
      - 'src/gaia/agents/base/agent.py'
      - 'src/gaia/agents/registry.py'
      - 'src/gaia/llm/lemonade_client.py'
      - 'src/gaia/llm/factory.py'
      - 'hub/agents/*/python/**'
      - 'tests/fixtures/eval_baselines/**'
      - '.github/workflows/test_eval_agent_gemma_consolidation.yml'
      # In-core agents that carry their own model_id, plus the prompt/tool
      # surfaces CLAUDE.md lists as eval-requiring — a tool docstring edit
      # changes the JSON tool schema and is exactly what tool_selection scores.
      # (ChatAgent is not listed: it now lives under hub/agents/chat/python/,
      # already covered above.)
      - 'src/gaia/agents/builder/**'
      - 'src/gaia/agents/tools/**'
      - 'src/gaia/chat/**'
      - 'src/gaia/eval/flagship_tasks.py'
      - 'eval/tasks/**'

concurrency:
  # Share the single Lemonade backend slot with the other self-hosted evals so two
  # runs never race-evict each other's model (CLAUDE.md: evals run serially).
  group: lemonade-eval
  cancel-in-progress: false
  # `queue: single` (the default) keeps only ONE pending run and cancels the
  # previously-queued one — a phantom red X on the PR. `max` queues them FIFO.
  queue: max

permissions:
  contents: read

env:
  BASELINE_DIR: tests/fixtures/eval_baselines/gemma-4-e4b-d71cd914
  BACKEND_URL: http://127.0.0.1:4200
  # This runner's home is C:\Windows\System32\config\systemprofile and the
  # checkout is not under it, so without this every scenario carrying a
  # `setup.index_documents` block 403s and scores as a failure (#3585).
  # Declares the checkout's corpus, nothing wider.
  GAIA_DOCUMENT_ROOTS: ${{ github.workspace }}\eval\corpus
  # A dispatch input always wins. Otherwise `[eval-enforce]` in the PR TITLE opts
  # in — workflow_dispatch is not offered until this file is on the default
  # branch, so without this there is no way to enforce pre-merge. Reads the PR
  # title, not `head_commit.message`: that context exists only on `push`, so it
  # is permanently null now that this triggers on `pull_request`.
  #
  # `edited` is deliberately NOT in the `types:` above — a title tweak must not
  # be able to launch a multi-hour run on the shared slot. So retitling alone
  # does not re-run the gate: add the tag, then push (or close/reopen).
  ENFORCE: ${{ github.event.inputs.enforce || (contains(github.event.pull_request.title, '[eval-enforce]') && 'true') || 'false' }}
  # PINNED TO THE BASELINE'S JUDGE, and that is not a stale value to bump.
  # `--model` is not a judge-only knob: runner.py passes it to `claude -p`, which
  # DRIVES every scenario as well as scoring it. The committed baselines record
  # `eval_model: claude-sonnet-4-6` (meta.json), so driving with anything else
  # compares two different agents-under-test and folds the new driver's wall
  # clock into the same >2x time verdict. #2859 moved every Claude workflow to
  # Opus 5; this one value is exempt until the baselines are regenerated with
  # that driver. Override per-run via the dispatch input above.
  EVAL_MODEL: ${{ github.event.inputs.eval_model || 'claude-sonnet-4-6' }}
  # Deep ladder is opt-in. Its rungs purge the embedder from disk and unload every
  # resident model, which is fine while investigating and wrong to repeat on every
  # PR: the cause is known (#3016) and the box is shared, so the next job landing
  # on it pays for a cold reload it did not ask for.
  DEEP_DIAG: ${{ github.event.inputs.deep_diagnostics || 'false' }}
  # Loopback services — bypass any runner proxy. This covers the PYTHON side
  # (httpx/requests in the eval, the agents and the backend); PowerShell's
  # Invoke-RestMethod ignores NO_PROXY and uses the system proxy instead.
  NO_PROXY: 'localhost,127.0.0.1'
  # Bare host (no /api/v1): LemonadeClient normalizes the suffix itself. Mirrors
  # test_email_agent_eval.yml so the preflight probe and the agents agree.
  LEMONADE_BASE_URL: 'http://127.0.0.1:13305'
  # The eval prints box-drawing characters and em dashes; the runner's default
  # console codepage is cp1252 and Python dies on the first one without this.
  PYTHONIOENCODING: 'utf-8'
  # Connector resolution blocks on the OS keyring in a headless runner, which
  # hangs agent construction. Same guard as test_email_agent_eval.yml.
  PYTHON_KEYRING_BACKEND: keyring.backends.null.Keyring
  # Persistent runner — keep long-term memory out of the loop so state cannot
  # bleed between the three chained categories or across runs. Established
  # convention (test_chat_agent.yml, test_email_agent_eval.yml).
  GAIA_MEMORY_DISABLED: '1'

jobs:
  # The flagship GaiaAgent on outcome-scored tasks (`gaia eval tasks`, docs:
  # docs/reference/eval.mdx), served by GAIA's own embedded Lemonade. Three steps
  # with separate environments: the agent runs with no credentials (it runs
  # shell commands), Claude judges quality (and decides the question tasks) in
  # one call with no tools, and the gate compares passes, quality, honesty,
  # tokens, steps and runtime with main's committed results in
  # eval/tasks/expectations/<model>.<suite>.json. The gate always enforces: a
  # missed expectation fails this job. A model with no committed expectations
  # is reported, not failed, and a task the backend could not serve is "not
  # measured", never a regression. Memory is off here, as for the whole workflow.
  # agent-eval waits for this job, so the one runner does them in a fixed order.
  flagship-tasks:
    name: Flagship agent tasks vs expectations
    if: >-
      github.event_name != 'pull_request' ||
      (github.event.pull_request.head.repo.full_name == github.repository &&
      (github.event.pull_request.draft == false ||
      contains(github.event.pull_request.labels.*.name, 'ready_for_ci')))
    runs-on: [self-hosted, Windows, strix-halo, lemonade-eval]
    defaults:
      run:
        # Windows PowerShell 5.1 and pure-ASCII run bodies, for the reasons
        # spelled out on the agent-eval job below.
        shell: powershell
    timeout-minutes: 90
    env:
      TASK_SUITE: ${{ github.event.inputs.task_suite || 'core' }}
      TASK_JUDGE_MODEL: claude-opus-5
      TASKS_OUT: eval-out\tasks
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Python Environment
        uses: ./.github/actions/setup-venv
        with:
          python-version: '3.12'
          # The flagship ships as its own hub package and builds on ChatAgent.
          install-package: '-e .[dev,eval,ui,api] -e hub/agents/chat/python -e hub/agents/gaia/python'

      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: '22'

      - name: Install Claude Code CLI (the judge)
        run: |
          npm install -g @anthropic-ai/claude-code
          if ($LASTEXITCODE -ne 0) { throw "Claude Code CLI install failed" }
          claude --version
          if ($LASTEXITCODE -ne 0) { throw "claude installed but not runnable on PATH" }

      - name: Start GAIA's embedded Lemonade
        timeout-minutes: 20
        run: |
          $ErrorActionPreference = "Continue"
          gaia lemonade embedded start --timeout 180
          if ($LASTEXITCODE -ne 0) {
            Get-Content "$HOME\.gaia\lemonade\lemond.log" -Tail 60 -ErrorAction SilentlyContinue
            throw "The embedded Lemonade did not start; its log is above."
          }
          gaia lemonade embedded install-backend llamacpp:vulkan
          if ($LASTEXITCODE -ne 0) { throw "Could not install llamacpp:vulkan into the embedded Lemonade." }
          gaia lemonade embedded status

      # No credentials in this step's environment: the agent runs shell commands.
      - name: Run the flagship on the task suite
        timeout-minutes: 60
        run: |
          $ErrorActionPreference = "Continue"
          [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
          . "$HOME\.gaia\lemonade\env.ps1"
          gaia eval tasks run --suite $env:TASK_SUITE --no-judge --out $env:TASKS_OUT
          if ($LASTEXITCODE -ne 0) { throw "gaia eval tasks run crashed (exit $LASTEXITCODE): a harness failure, not a verdict." }

      - name: Judge quality
        if: ${{ !cancelled() }}
        timeout-minutes: 30
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          # Blank when the OAuth token is set: the judge adds --bare only with a key.
          ANTHROPIC_API_KEY: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN == '' && secrets.ANTHROPIC_API_KEY || '' }}
        run: |
          [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
          gaia eval tasks judge $env:TASKS_OUT --judge-model $env:TASK_JUDGE_MODEL --judge-attempts 2
          if ($LASTEXITCODE -ne 0) { throw "gaia eval tasks judge failed (exit $LASTEXITCODE)." }

      # Always enforced. With no committed expectations for the model yet it only
      # reports; the proposal in the artifact is the file to commit from main.
      - name: Gate on the committed expectations
        if: ${{ !cancelled() }}
        run: |
          [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
          gaia eval tasks gate $env:TASKS_OUT --enforce --propose "$env:TASKS_OUT\proposed-expectations.json"
          exit $LASTEXITCODE

      - name: Stop the embedded Lemonade
        if: always()
        run: gaia lemonade embedded stop

      # The artifact is public and, unlike the job log, not secret-masked.
      - name: Refuse to upload a credential
        if: always()
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          $ErrorActionPreference = "Stop"
          $files = @(Get-ChildItem $env:TASKS_OUT -File -Include scorecard.json, proposed-expectations.json -Recurse -ErrorAction SilentlyContinue)
          $leaked = @()
          foreach ($name in @("CLAUDE_CODE_OAUTH_TOKEN", "ANTHROPIC_API_KEY")) {
            $value = [Environment]::GetEnvironmentVariable($name)
            if (-not $value) { continue }
            foreach ($file in $files) {
              if ((Test-Path -LiteralPath $file.FullName) -and (Select-String -LiteralPath $file.FullName -SimpleMatch -Pattern $value -Quiet)) {
                $leaked += $file.FullName
                Remove-Item -LiteralPath $file.FullName -Force
              }
            }
          }
          if ($leaked.Count -gt 0) {
            Write-Host "::error::A credential appeared in $($leaked.Count) file(s) bound for the public artifact. They were deleted, not uploaded: $($leaked -join ', ')"
            exit 1
          }
          Write-Host "Scanned $($files.Count) file(s): no credential found."

      # Scores only: transcripts and diffs stay on the runner; reproduce locally.
      - name: Upload the scorecard
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: flagship-tasks
          path: |
            eval-out/tasks/scorecard.json
            eval-out/tasks/proposed-expectations.json
          if-no-files-found: warn
          retention-days: 14

  agent-eval:
    name: rag_quality + context_retention + tool_selection vs Gemma baselines
    # After flagship-tasks whatever its result (`!cancelled()` below): the two
    # jobs share one runner, so the order is fixed rather than incidental.
    needs: flagship-tasks
    # Fork PRs and drafts never reach the lab pool. A fork PR would schedule
    # untrusted code onto a self-hosted runner and could not pass anyway
    # (ANTHROPIC_API_KEY is not exposed to fork PRs, so the judge preflight
    # fails); a draft would spend a multi-hour single-slot run on work still in
    # progress. A job-level `if` resolves before a runner is assigned, so a
    # skipped job never reaches the pool at all. `ready_for_ci` is the repo-wide
    # escape hatch for a draft that does want CI. Mirrors the guard in
    # test_email_agent_eval.yml.
    #
    # Kept flat-indented on purpose: a YAML folded scalar preserves newlines on
    # more-indented lines, and an `if:` expression must fold to a single line.
    if: >-
      (github.event_name != 'pull_request' ||
      (github.event.pull_request.head.repo.full_name == github.repository &&
      (github.event.pull_request.draft == false ||
      contains(github.event.pull_request.labels.*.name, 'ready_for_ci')))) &&
      !cancelled()
    # `Windows` is load-bearing, not decoration: self-hosted pool labels here also
    # match a Linux box (xsj-aimlab-stxp-04), and every step below is PowerShell +
    # LemonadeServer.exe. Landing on Linux dies with 'powershell: command not
    # found'. Same guard as test_email_agent_eval.yml.
    #
    # `strix-halo` pins the SoC, and that is a correctness requirement, not a
    # preference: the `stx` pool spans Strix Point boxes as well (sjlab-stx-1,
    # sjlab-stx-3), which do not have the 128 GB of unified memory this eval's
    # model needs, and scores from two different SoCs are not comparable to each
    # other or to the committed baselines. Do NOT widen this to `stx` — an
    # earlier revision of this comment suggested exactly that, which would have
    # let a scorecard be produced on whichever machine happened to be free.
    #
    # `lemonade-eval` is kept alongside it to say WHICH Halo box owns the long
    # eval slot. It is redundant while exactly one runner carries both, and that
    # is the point: if the label is ever moved to a non-Halo box this job finds
    # no runner and queues visibly, rather than quietly measuring the wrong
    # hardware. Serialisation still comes from the concurrency group, not either
    # label. Same `Windows` guard as the sibling evals: the pool also matches a
    # Linux box (xsj-aimlab-stxp-04) and every step here is PowerShell.
    runs-on: [self-hosted, Windows, strix-halo, lemonade-eval]
    defaults:
      run:
        # Pinned to Windows PowerShell 5.1, not left to the runner default (pwsh
        # when installed, powershell otherwise). Everything below is written to
        # 5.1's rules — the Tee-Object/`2>&1` and $LASTEXITCODE notes are
        # 5.1-specific — and this file's first run died because nothing declared
        # a shell at all and the steps were bash.
        #
        # KEEP EVERY `run:` BODY PURE ASCII. Actions writes the block to a .ps1
        # with no BOM, and PowerShell 5.1 reads a BOM-less file as ANSI, not
        # UTF-8 — so one em dash inside a `run:` string is three cp1252 bytes
        # mid-token and the whole step dies at parse time before a line executes.
        # (Run 32274575191: a single `—` in an ::error:: message produced
        # "Missing argument in parameter list" 30 lines away from itself.) YAML
        # comments and step `name:` values are fine; they never reach the file.
        shell: powershell
    # BUDGET DERIVATION — measured on this runner, not guessed.
    #
    # The first clean run (30869194618) was CANCELLED by a hand-picked
    # `timeout-minutes: 150` after 148 min, having finished exactly ONE of three
    # categories. It produced the only real numbers this pool has:
    #
    #   setup (checkout -> preflight, inclusive)                    0:01:18
    #   rag_quality        8 scenarios                            2:04:07  measured
    #   context_retention  1 of 4 scenarios before the cancel      0:23:06  partial
    #   tool_selection     never reached                                 —
    #
    # rag_quality alone ran 124 min against a 22.4-min committed baseline — 5.5x,
    # because that baseline was captured on different hardware over a remote
    # Lemonade (see meta.json). The old 150 was sized off the ~55-min baseline sum
    # and never had a chance. Per-category CEILINGS from the measured rates:
    #
    #   rag_quality        measured 124                                 -> 150
    #   context_retention  4 x the 23.1-min scenario measured above = 92 -> 110
    #   tool_selection     4 scenarios, same shape as context_retention  -> 110
    #   eval step ceiling  150+110+110 = 370                            -> 400
    #   setup (the Lemonade start step already carries its own 15-min cap) -> 20
    #   collect + integrity + compare + upload                          -> 15
    #   job: 400+20+15 = 435                                            -> 450
    #
    # These are CEILINGS, not expectations — expected wall clock is ~5h. Sizing
    # generously is close to free here: the pool is self-hosted (no per-minute
    # billing, and the 6h job cap is a GitHub-HOSTED limit; self-hosted is 5 days),
    # and the shared `lemonade-eval` concurrency group already serialises the pool,
    # so a high ceiling cannot let this job trample another eval. The asymmetry is
    # what matters: too low costs hours of runner time AND the evidence, too high
    # costs nothing. email_scorecard_refresh.yml sits at 840 on this same pool for
    # exactly this reason (#2094: a hand-typed 90 against a measured 2h workload
    # burned 100 consecutive dispatches without one completing).
    #
    # The eval step carries its OWN timeout-minutes below so an overrun fails at a
    # NAMED step instead of a silent job-level `cancelled` — a job-level timeout
    # skips the `!cancelled()` collect/integrity/compare steps, which is precisely
    # how run 30869194618 threw away the 124 min of rag_quality data it had already
    # produced. Re-tighten both once a few complete runs establish the true wall
    # time; the per-category [TIMING] lines in the eval step exist to supply it.
    timeout-minutes: 450
    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Setup Python Environment
        uses: ./.github/actions/setup-venv
        with:
          python-version: '3.12'
          # [dev] + [eval] for the eval harness and the Anthropic judge deps;
          # [ui] for fastapi/uvicorn + the RAG deps gaia.ui.server imports at boot
          # ([dev] alone fails at import), [api] for the connectors layer.
          # ChatAgent ships as the standalone gaia-agent-chat wheel (#1102) and is
          # NOT pulled by those extras — gaia.ui.server imports gaia_agent_chat to
          # build the backend-default agent these scenarios run against, so install
          # the LOCAL editable hub package or every scenario dies at session
          # creation. Core gaia stays editable so eval fixtures resolve in-repo.
          # `mcp` is not optional for this gate: every scenario is driven through
          # the Agent UI MCP server, and without the extra that server exits with
          # ModuleNotFoundError before writing a protocol frame — which the client
          # reports only as CONNECTION_CLOSED.
          install-package: '-e .[dev,eval,ui,api,mcp] -e hub/agents/chat/python'

      # `gaia eval agent` does not merely call the Anthropic API - it shells out
      # to `claude -p` with an MCP config to DRIVE each scenario (runner.py:937),
      # and its own preflight hard-fails with "'claude' CLI not found on PATH"
      # before running anything. ANTHROPIC_API_KEY is necessary but not
      # sufficient; the CLI has to be installed too. Same prerequisite the
      # eval-CI recipe documents (docs/guides/eval-ci.mdx).
      - name: Set up Node
        uses: actions/setup-node@v7
        with:
          node-version: '22'

      - name: Install Claude Code CLI (the eval's scenario driver)
        run: |
          npm install -g @anthropic-ai/claude-code
          if ($LASTEXITCODE -ne 0) { throw "Claude Code CLI install failed" }
          # Fail here, not after the backend has booted.
          claude --version
          if ($LASTEXITCODE -ne 0) { throw "claude installed but not runnable on PATH" }

      - name: Install Lemonade Server
        uses: ./.github/actions/install-lemonade

      - name: Start Lemonade Server (version-matched, warmed)
        timeout-minutes: 15
        env:
          HUGGINGFACE_ACCESS_TOKEN: ${{ secrets.HUGGINGFACE_ACCESS_TOKEN }}
          HF_TOKEN: ${{ secrets.HUGGINGFACE_ACCESS_TOKEN }}
        run: |
          # Launches the version-matched server as a persistent Scheduled Task
          # (survives across CI jobs), reuses it if healthy, and warms the model.
          # Default -WarmModel is already Gemma-4-E4B-it-GGUF, the model under test.
          powershell -ExecutionPolicy Bypass -File installer\scripts\ensure-lemonade-running.ps1
          if ($LASTEXITCODE -ne 0) { throw "Lemonade server not ready" }

      - name: Preflight — judge key, baselines, Lemonade
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          # Blank when the OAuth token is present, deliberately: runner.py adds
          # `--bare` only when ANTHROPIC_API_KEY is set, and `--bare` restricts
          # auth to the key alone, so leaving both set would ignore the token.
          ANTHROPIC_API_KEY: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN == '' && secrets.ANTHROPIC_API_KEY || '' }}
        run: |
          $ErrorActionPreference = "Stop"
          $ProgressPreference = "SilentlyContinue"

          # One probe, so each rung of the ladder below runs an identical check.
          # $LlamacppArgs empty = load the embedder bare, which is ONLY ever used
          # as the discriminator on the last rung -- never to take the job green,
          # because production RAG always passes --ubatch-size 2048
          # (gaia/rag/sdk.py::_load_embedder) and measuring a different load than
          # production performs is exactly the "we did not measure" case the
          # integrity gate exists to refuse.
          function Invoke-EmbedProbe {
            param([string]$LlamacppArgs)
            $ErrorActionPreference = "Continue"
            $global:LASTEXITCODE = 0
            if ($LlamacppArgs) {
              $out = & python tests/ci_lemonade_check.py `
                --model $embeddingModel --checkpoint $embeddingCheckpoint `
                --recipe $embeddingRecipe --register-embedding --embeddings `
                --llamacpp-args $LlamacppArgs 2>&1
            } else {
              $out = & python tests/ci_lemonade_check.py `
                --model $embeddingModel --checkpoint $embeddingCheckpoint `
                --recipe $embeddingRecipe --register-embedding --embeddings 2>&1
            }
            $code = $LASTEXITCODE
            $ErrorActionPreference = "Stop"
            $out | ForEach-Object { Write-Host "$_" }
            # Assert the EMBEDDINGS line, not the helper's terminal `[ci] OK` -
            # that one prints whether or not --embeddings was passed. `dim=` only
            # prints after a real vector came back. Exit 0 alone is not enough
            # either: a command that fails to LAUNCH leaves $LASTEXITCODE at the
            # reset 0 and would read as a pass.
            return ($code -eq 0 -and [bool]($out -match '\[ci\] embeddings OK \(dim='))
          }

          # Lemonade runs as a SYSTEM Scheduled Task, whose action has no console
          # and whose stdout/stderr the scheduler records nowhere -- so until
          # ensure-lemonade-running.ps1 began redirecting them, llama-server's
          # startup error was discarded by the OS, not merely hard to locate.
          # Every #2920 failure reported "(none found)" from a SYSTEM-profile glob
          # that never had anything to match. Read the redirect target instead.
          function Show-LemonadeTaskLog {
            $dir = "C:\ProgramData\GaiaLemonadeServer"
            Write-Host "=== Lemonade task stdout/stderr (tail) ==="
            $found = $false
            foreach ($f in @("$dir\lemonade-task-stderr.log", "$dir\lemonade-task-stdout.log")) {
              if (Test-Path $f) {
                # Report the size: "file exists but is empty" and "file is absent"
                # are different faults (buffering vs. no redirect) and printing
                # nothing for both makes them indistinguishable.
                $len = (Get-Item $f).Length
                Write-Host "--- $f ($len bytes) ---"
                if ($len -gt 0) {
                  Get-Content $f -Tail 120 -ErrorAction SilentlyContinue | ForEach-Object { Write-Host "$_" }
                  $found = $true
                } else {
                  Write-Host "(empty - server captured nothing on this stream)"
                }
              }
            }
            if (-not $found) {
              Write-Host "(none found in $dir - this runner's Scheduled Task predates the redirect; the -ForceRestart below re-registers it with logging)"
            }
            return $found
          }

          # Lemonade's POST /delete deregisters a model but never removes its
          # files, so the probe's delete + re-pull cannot re-fetch anything: it
          # short-circuits on the blob cache and reloads the same bytes every
          # time. Freeing them needs an OS-level delete, which is this function.
          # Roots follow _huggingface_cache_dir / _lemonade_models_dir in
          # src/gaia/installer/uninstall_command.py; report what was removed, since
          # the sizes are the evidence that a re-pull was ever real.
          function Clear-EmbedderFromDisk {
            $sys = "$env:SystemRoot\System32\config\systemprofile"
            $roots = @(
              "$sys\AppData\Local\huggingface\hub", "$sys\.cache\huggingface\hub",
              "$sys\AppData\Local\lemonade\models",  "$sys\.cache\lemonade\models",
              "$env:LOCALAPPDATA\huggingface\hub",   "$env:USERPROFILE\.cache\huggingface\hub",
              "$env:LOCALAPPDATA\lemonade\models",   "$env:USERPROFILE\.cache\lemonade\models"
            )
            if ($env:HF_HOME) { $roots += (Join-Path $env:HF_HOME "hub") }
            $removed = 0
            foreach ($root in ($roots | Where-Object { $_ } | Select-Object -Unique)) {
              if (-not (Test-Path $root)) { continue }
              Get-ChildItem $root -Filter "*embeddinggemma*" -Directory -ErrorAction SilentlyContinue |
                ForEach-Object {
                  $mb = 0
                  try {
                    $mb = [math]::Round(((Get-ChildItem $_.FullName -Recurse -File -ErrorAction SilentlyContinue |
                          Measure-Object Length -Sum).Sum / 1MB), 1)
                  } catch { }
                  Write-Host "Removing on-disk embedder cache: $($_.FullName) ($mb MB)"
                  Remove-Item $_.FullName -Recurse -Force -ErrorAction SilentlyContinue
                  $removed++
                }
            }
            if ($removed -eq 0) {
              # Do NOT read this as "the delete worked" - it never removes files.
              # Nothing found almost certainly means the cache lives somewhere the
              # roots above do not cover, so the retry below is still loading the
              # same bytes and this probe proved nothing.
              Write-Host "PURGE: found nothing to remove. Lemonade's delete does not remove files, so this most likely means the model cache is NOT under any of these roots, not that the disk was clean: $($roots -join '; ')"
            } else {
              Write-Host "PURGE: removed $removed dir(s). Lemonade's delete only deregisters, so this is the first time the bytes are actually gone and the next pull genuinely re-fetches."
            }
            return $removed
          }


          if (-not $env:CLAUDE_CODE_OAUTH_TOKEN -and -not $env:ANTHROPIC_API_KEY) {
            Write-Host "::error::Neither CLAUDE_CODE_OAUTH_TOKEN nor ANTHROPIC_API_KEY is set. `gaia eval agent` drives AND scores each scenario through ``claude -p`` (src/gaia/eval/runner.py), so without a credential every scenario errors in a few seconds and the scorecard is meaningless. Add either repository secret, or run the eval locally on AMD hardware."
            exit 1
          }

          # Assert the credential is ACCEPTED, not merely present. #3341 sat red
          # for weeks because the key existed and the account behind it was out
          # of credit: the preflight passed and all five scenarios then died in
          # three seconds each with their stdout discarded.
          claude -p --model $env:EVAL_MODEL "reply with: ok" 2>&1 | Out-String -OutVariable probe | Out-Null
          if ($LASTEXITCODE -ne 0) {
            Write-Host "::error::The Claude credential is present but a one-line probe was rejected, so every scenario would error. Response: $probe"
            exit 1
          }
          Write-Host "Claude credential accepted by a live probe."

          foreach ($category in @("rag_quality", "context_retention", "tool_selection")) {
            $baseline = Join-Path $env:BASELINE_DIR "scorecard_$category.json"
            if (-not (Test-Path $baseline)) {
              Write-Host "::error::Missing baseline $baseline. Pick the baseline matching the model under test; do not substitute another directory."
              exit 1
            }
            Write-Host "baseline ok: $baseline"
          }

          # Resolve the URL and the model under test through the client's own
          # constants rather than hardcoding them here: the default port already
          # moved once (8000 -> 13305 in Lemonade v10.1.0) and a stale literal
          # would fail a healthy runner.
          # The embedder's checkpoint/recipe come from the same MODELS entry
          # gaia.rag.sdk registers it from, so the probe below cannot drift from
          # what RAG will actually ask Lemonade for.
          $global:LASTEXITCODE = 0
          $probe = @(python -c "from gaia.llm.lemonade_client import DEFAULT_EMBEDDING_MODEL, DEFAULT_MODEL_NAME, MODELS, _get_lemonade_config; m = next(x for x in MODELS.values() if x.model_id == DEFAULT_EMBEDDING_MODEL); print(_get_lemonade_config()[2]); print(DEFAULT_MODEL_NAME); print(DEFAULT_EMBEDDING_MODEL); print(m.checkpoint); print(m.recipe)")
          if ($LASTEXITCODE -ne 0 -or $probe.Count -lt 5) {
            Write-Host "::error::Could not resolve the Lemonade URL / models under test - the venv install is broken."
            exit 1
          }
          $lemonadeUrl = $probe[-5]
          $modelUnderTest = $probe[-4]
          $embeddingModel = $probe[-3]
          $embeddingCheckpoint = $probe[-2]
          $embeddingRecipe = $probe[-1]

          try {
            Invoke-RestMethod -Uri "$lemonadeUrl/health" -TimeoutSec 10 | Out-Null
          } catch {
            Write-Host "::error::Lemonade Server not reachable at $lemonadeUrl. The previous step should have started it; check its log, start it on the runner (installer\scripts\ensure-lemonade-running.ps1), or set LEMONADE_BASE_URL to a reachable instance."
            exit 1
          }
          Write-Host "Lemonade reachable at $lemonadeUrl"

          # Record WHICH machine produced this scorecard. The runs-on labels state
          # the intent; this is the only thing that proves it held at runtime, and
          # a score is not interpretable without it - the pool spans two SoCs and
          # only Strix Halo has the memory this eval assumes. /v1/system-info also
          # carries per-recipe backend state (unsupported / installable /
          # update_required / installed) and compatible devices, which is the
          # diff that explains why this embedder loads on a Strix Point box and
          # not here (#3016). Best-effort and never fatal: it is evidence, not a
          # gate, and must not be able to fail a run on its own.
          $ErrorActionPreference = "Continue"
          Write-Host "=== Lemonade system-info (device + llamacpp backend state) ==="
          python -c "import json; from gaia.llm.lemonade_client import LemonadeClient; si = LemonadeClient().get_system_info(); print(json.dumps({k: v for k, v in si.items() if k != 'recipes'}, indent=2, default=str)[:2000]); print('--- recipes.llamacpp ---'); print(json.dumps(si.get('recipes', {}).get('llamacpp'), indent=2, default=str)[:2000])"
          if ($LASTEXITCODE -ne 0) { Write-Host "(system-info unavailable on this Lemonade build - continuing, this is diagnostic only)" }
          $global:LASTEXITCODE = 0
          $ErrorActionPreference = "Stop"

          # ensure-lemonade-running.ps1 SWALLOWS a failed warm pull ("WARN: warm
          # pull failed") and still exits 0, so a healthy server proves nothing
          # about the model this job exists to measure. `/models` (no show_all)
          # lists DOWNLOADED models only, so this is the assertion that the model
          # is actually on disk. Without it a run can score every scenario against
          # whatever was resident and compare it to the Gemma baseline.
          try {
            $downloaded = @((Invoke-RestMethod -Uri "$lemonadeUrl/models" -TimeoutSec 60).data | ForEach-Object { $_.id })
          } catch {
            Write-Host "::error::Could not list downloaded models at $lemonadeUrl/models. Lemonade answered /health but not /models - check the server log on the runner."
            exit 1
          }
          if ($downloaded -notcontains $modelUnderTest) {
            Write-Host "::error::$modelUnderTest is not downloaded on this runner (Lemonade has: $($downloaded -join ', ')). The warm-pull step failed silently. Pull it on the runner (POST $lemonadeUrl/pull with model_name=$modelUnderTest) and re-run; do NOT let this eval score a different model against the Gemma baseline."
            exit 1
          }
          Write-Host "Model under test present: $modelUnderTest"

          # ...but "present" only means the GGUF is on disk. Nothing in this job
          # has ever LOADED it: ensure-lemonade-running.ps1 warms via /pull, which
          # is a download, and the check above reads /models' `downloaded` flag. So
          # a runner where llama-server cannot start AT ALL looks identical here to
          # a healthy one, and the first thing to actually load a model is the
          # embedder probe below -- which is why an embedder-specific fault and a
          # dead llama-server have been indistinguishable for this gate's whole
          # history (#2920). Load the CHAT model first: it shares the llama-server
          # binary, so if it also fails, the embedder is not the variable.
          $ErrorActionPreference = "Continue"
          $global:LASTEXITCODE = 0
          $chatOut = & python tests/ci_lemonade_check.py --model $modelUnderTest --chat 2>&1
          $chatCode = $LASTEXITCODE
          $ErrorActionPreference = "Stop"
          $chatOut | ForEach-Object { Write-Host "$_" }
          if ($chatCode -ne 0 -or -not ($chatOut -match '\[ci\] chat OK')) {
            Write-Host "::error::llama-server cannot start for the CHAT model $modelUnderTest on this runner either, so this is not an embedder fault - no model can be served here and every category would fail. The GGUF is downloaded and Lemonade answers /health, so the server process is up and the binary underneath it is what will not spawn. Check whether an Application Control policy is blocking llama-server.exe on this box (#2826), and read the Lemonade task log below."
            Show-LemonadeTaskLog | Out-Null
            exit 1
          }
          Write-Host "Chat model loads and serves - llama-server works on this runner, so an embedder failure below is embedder-specific."

          # THE RAG CATEGORIES RUN ON A SECOND MODEL. rag_quality and
          # context_retention answer entirely out of chunks the EMBEDDER
          # produced, so an embedder that cannot start turns every doc-grounded
          # scenario into a ~2/10 non-answer that reads exactly like a model
          # regression. ensure-lemonade-running.ps1 warms only the chat model
          # and nothing asserted the embedder, so run 31051933420 spent 3h31m
          # proving it: llama-server returned model_load_error on every index,
          # RAG got 0 chunks, and rag_quality scored 3.1 against a 9.4 baseline
          # captured with the SAME chat model. Probe it here, in seconds.
          #
          # Probed AFTER the chat model is warm on purpose: the embedder is
          # co-resident with it (#1544), so probing an idle server would not
          # reproduce the condition the eval actually runs under.
          #
          # Presence in /models is not enough - it proves the GGUF is on disk,
          # not that llama-server can serve it, and "on disk but unservable" is
          # the exact failure above. tests/ci_lemonade_check.py is the shared CI
          # helper for precisely this (test_embeddings.yml, test_rag.yml,
          # test_api.yml all use it, and it is green on the sibling stx runners),
          # so this reuses it rather than hand-rolling a weaker probe:
          #
          #   --register-embedding  DELETES any prior registration and re-registers
          #     with the embeddings label before pulling. That is the one failure
          #     in this class CI can actually REPAIR: a stale registration left by
          #     an earlier job (wrong recipe, missing label - #1745) makes every
          #     subsequent load fail forever, and ensure_model_downloaded alone
          #     short-circuits on "already downloaded" and never re-applies it.
          #   --llamacpp-args       loads with the SAME flag gaia.rag.sdk passes
          #     (_load_embedder: --ubatch-size 2048). Without it the probe proves
          #     the embedder can be served, not that RAG's load of it succeeds -
          #     which is the difference between a green preflight and a green run.
          #   the client's load retry absorbs the known transient AMD-Vulkan
          #     "llama-server failed to start" fault instead of tripping on it.
          #
          # Continue, not Stop: under Stop the helper's first stderr line becomes
          # a terminating NativeCommandError and the step dies before the
          # ::error:: below. Same reasoning as the eval step's own note.
          # RUNG 1 - the load production performs.
          $embedOk = Invoke-EmbedProbe -LlamacppArgs "--ubatch-size 2048"

          # RUNG 2 - a stale llama.cpp backend cannot spawn and reports exactly
          # this "llama-server failed to start".
          if (-not $embedOk) {
            Show-LemonadeTaskLog | Out-Null

            # A stale llama.cpp backend in Lemonade's bin cache cannot spawn, and
            # reports exactly this "llama-server failed to start". install-lemonade
            # already wipes that cache - but only when it reconciles, so a runner
            # whose VERSION is correct never clears it and can never self-heal
            # (#2920). Do the same wipe here, restart, and re-probe once.
            Write-Host "=== Probe failed; clearing the llama.cpp backend cache and retrying once ==="
            $cacheRoots = @(
              "$env:SystemRoot\System32\config\systemprofile\.cache\lemonade",
              "$env:USERPROFILE\.cache\lemonade"
            )
            if ($env:LEMONADE_CACHE_DIR) { $cacheRoots += $env:LEMONADE_CACHE_DIR }
            $seenRoots = @{}
            foreach ($root in ($cacheRoots | Where-Object { $_ })) {
              $rootKey = $root.ToLowerInvariant()
              if ($seenRoots.ContainsKey($rootKey)) { continue }
              $seenRoots[$rootKey] = $true
              $backendDir = Join-Path $root "bin"
              if (-not (Test-Path $backendDir)) { continue }
              Write-Host "Clearing stale llama.cpp backend cache at $backendDir"
              # Cached ROCm Composable-Kernel headers blow past MAX_PATH, so mirror
              # an empty dir over the tree with robocopy (long-path-safe) first.
              $emptyDir = Join-Path $env:RUNNER_TEMP "lemonade-wipe-empty"
              if (-not (Test-Path $emptyDir)) { New-Item -ItemType Directory -Path $emptyDir -Force | Out-Null }
              robocopy $emptyDir $backendDir /MIR /NFL /NDL /NJH /NJS /NP | Out-Null
              # robocopy: <8 is success. Fail loud - a swallowed wipe leaves the
              # stale backend in place and the retry below repeats the failure.
              if ($LASTEXITCODE -ge 8) { throw "robocopy mirror-wipe of $backendDir failed with exit code $LASTEXITCODE" }
              Remove-Item $backendDir -Recurse -Force -ErrorAction Stop
            }
            $global:LASTEXITCODE = 0

            Write-Host "Restarting Lemonade so it refetches a matching backend..."
            powershell -ExecutionPolicy Bypass -File installer\scripts\ensure-lemonade-running.ps1 -ForceRestart
            $global:LASTEXITCODE = 0

            $embedOk = Invoke-EmbedProbe -LlamacppArgs "--ubatch-size 2048"
            if ($embedOk) {
              Write-Host "Embedder recovered after the backend-cache wipe - the cached llama.cpp backend was stale."
            }
          }

          # STOP HERE ON A NORMAL RUN. Everything above is cheap and read-only:
          # it establishes whether llama-server works at all on this box and
          # whether a stale backend was the problem. Everything below purges the
          # model from disk and unloads every resident model, which was the right
          # thing to do while the cause was unknown and is the wrong thing to
          # repeat on every PR now that it is known (#3016) - it adds ~7 min and
          # leaves the next job on this shared box paying for a cold reload.
          # Re-dispatch with deep_diagnostics: true to run the full ladder.
          # ...and do NOT kill the job here. Exiting left PRs with no regression
          # signal whatsoever; record the state, let the run measure what it can,
          # and let the integrity gate render the honest verdict.
          #
          # How much tool_selection survives a dead embedder is LESS than an
          # earlier revision of this comment claimed. It asserted the category
          # "carries no corpus at all"; three of its five scenarios
          # (data_vs_recall_disambiguation, known_path_read, multi_step_plan) index
          # corpus documents in their `setup:`, so they need the embedder too. Only
          # no_tools_needed and smart_discovery are genuinely corpus-free. Running
          # the category anyway is still worth it - two measured scenarios beat
          # zero - but do not read a green tool_selection here as full coverage;
          # the integrity gate names exactly what was and was not measured.
          #
          # `exit 0` is what makes both of those true, and it is load-bearing:
          # without it this block only PRINTED the promise. Execution fell through
          # into the rungs below and ended at their terminal `exit 1`, which failed
          # the step, which skipped the eval step entirely - so tool_selection was
          # never measured either and the gate blocked PRs while reporting nothing
          # (run 33286140041: 12 min of preflight, three categories skipped).
          #
          # The annotation level tracks `enforce` for the same reason the integrity
          # gate's does: #3016 is an environment defect, so on a report-mode PR run
          # a red ::error:: on a green job is noise that trains people to ignore the
          # check. The text is identical either way - only its severity moves.
          if (-not $embedOk -and $env:DEEP_DIAG -ne 'true') {
            Show-LemonadeTaskLog | Out-Null
            "EMBEDDER_OK=false" | Out-File -FilePath $env:GITHUB_ENV -Append -Encoding utf8
            $level = if ($env:ENFORCE -eq 'true') { 'error' } else { 'warning' }
            Write-Host "::${level}::The RAG embedder $embeddingModel will not load on this runner - a known Strix Halo limitation tracked in #3016, NOT a fault in this PR. The chat model loaded and served over the same llama-server binary earlier in this step, and the same embedder loads fine on the Strix Point boxes, so llama-server works here and the gap is specific to the embeddings path on this SoC. Re-running this gate will not clear it. rag_quality and context_retention cannot be measured at all; tool_selection WILL still run, but only its two corpus-free scenarios (no_tools_needed, smart_discovery) can produce a measurement - the other three index documents and need the same embedder. The deeper rungs (purge the model from disk, load without --ubatch-size 2048, load with everything else unloaded) are skipped on PR runs on purpose because they disturb other jobs on this shared box - re-dispatch with deep_diagnostics: true if you need the full elimination ladder."
            exit 0
          }

          # RUNG 3 - purge the model's BYTES, not just its registration, then
          # retry the production load once. `--register-embedding` already deletes
          # and re-pulls before every attempt above, so a third identical retry
          # would prove nothing; what has never been tried is removing the files
          # underneath, because Lemonade's delete may only drop the registry entry
          # and leave the HF blob cache intact (`gaia uninstall --purge-hf-cache`
          # exists for exactly that). If a corrupt or truncated GGUF is the cause,
          # this is the only rung that can clear it - and it takes the job green,
          # which the diagnostic rung below deliberately never does.
          if (-not $embedOk) {
            Write-Host "=== Backend-cache wipe did not help; purging the embedder from disk and retrying once ==="
            Clear-EmbedderFromDisk | Out-Null
            Write-Host "Restarting Lemonade before the clean-disk retry..."
            powershell -ExecutionPolicy Bypass -File installer\scripts\ensure-lemonade-running.ps1 -ForceRestart
            $global:LASTEXITCODE = 0
            $embedOk = Invoke-EmbedProbe -LlamacppArgs "--ubatch-size 2048"
            if ($embedOk) {
              Write-Host "Embedder recovered after a full on-disk purge - the cached GGUF was bad. Continuing."
            }
          }

          # RUNG 4 - DIAGNOSTIC ONLY, never a pass. Everything shared with the two
          # healthy sibling jobs (test_embeddings.yml, test_rag.yml - same model,
          # same helper, green on their runner) has now been ruled out, and the
          # remaining differences are this job's --ubatch-size 2048 and its
          # co-resident chat model. Loading bare separates those in one run
          # instead of another week of blind re-runs. It cannot take the job
          # green: production RAG always passes the flag (gaia/rag/sdk.py), so a
          # bare load measures something GAIA never does.
          if (-not $embedOk) {
            Show-LemonadeTaskLog | Out-Null
            Write-Host "=== Discriminating probe: loading the embedder WITHOUT --ubatch-size 2048 (diagnostic only) ==="
            $bareOk = Invoke-EmbedProbe
            $logged = Show-LemonadeTaskLog
            $logHint = if ($logged) { "llama-server's own error is in the task-log tail above" } else { "no task log was captured even after a -ForceRestart re-register - check that this runner's ensure-lemonade-running.ps1 carries the stdout/stderr redirect" }

            if ($bareOk) {
              Write-Host "::error::The RAG embedder $embeddingModel loads FINE without ``--ubatch-size 2048`` and fails WITH it, on this runner. That flag is not CI-only - gaia/rag/sdk.py::_load_embedder passes it on every embedder load, so document Q&A and every RAG-backed agent are broken on this hardware the same way, and this eval gate has simply never been able to measure them (0 passes in its history, #2920). Fix the load in gaia/rag/sdk.py - do NOT drop the flag from this probe to go green, which would measure a load GAIA never performs. $logHint"
              exit 1
            }

            # RUNG 5 - the last hypothesis standing. The chat model loaded and
            # served over the same llama-server binary earlier in this step, so
            # the box can spawn it; the flag, the registration, the model's bytes
            # and the backend cache are all eliminated above. What differs from
            # the two green sibling jobs is that they load this embedder on a
            # fresh `-NoModel` server while here a chat model is already resident
            # (#1544 keeps it co-resident by design). Evict everything and load
            # the embedder alone: if that works, co-residency is the cause, and
            # on unified-memory hardware that is a GAIA problem, not a CI one.
            Write-Host "=== Final discriminator: unloading ALL models, then loading the embedder ALONE ==="
            $ErrorActionPreference = "Continue"
            python -c "from gaia.llm.lemonade_client import LemonadeClient; LemonadeClient().unload_model(ignore_if_not_loaded=True); print('[ci] unloaded all models')"
            $ErrorActionPreference = "Stop"
            $aloneOk = Invoke-EmbedProbe -LlamacppArgs "--ubatch-size 2048"
            Show-LemonadeTaskLog | Out-Null
            if ($aloneOk) {
              Write-Host "::error::The RAG embedder $embeddingModel loads ALONE but not while the chat model is resident. This runner cannot hold both at once, and gaia/rag/sdk.py::_load_embedder deliberately keeps the chat model resident (#1544), so on this hardware every RAG-backed agent hits the same wall, and this gate has never been able to measure them. Fix the co-residency in the RAG SDK (evict-then-reload, or a smaller resident context); do NOT paper over it here, since an eval that unloads the chat model measures a configuration GAIA never runs."
              exit 1
            }

            Write-Host "::error::The RAG embedder $embeddingModel cannot be served on this runner under any condition tried. Everything cheap is now eliminated, so do not re-run hoping it clears: the chat model LOADED and served over the same llama-server binary earlier in this step (so the binary spawns fine here and this is not #2826), the load fails identically with and without ``--ubatch-size 2048``, alone and co-resident with the chat model, and the registration, the model's on-disk bytes and the llama.cpp backend cache were each purged and retried. What is left is this specific embedding model on this specific box - most likely the ``--embeddings``/pooling path of the llama.cpp build Lemonade ships here, which is the same class of fault as #1831. Next step is a HUMAN one: on the runner, run Lemonade in a console (not the Scheduled Task, whose stdout/stderr it does not write to) and read llama-server's own failure while reproducing with ``python tests/ci_lemonade_check.py --model $embeddingModel --checkpoint $embeddingCheckpoint --recipe $embeddingRecipe --register-embedding --embeddings --llamacpp-args '--ubatch-size 2048'``. $logHint. Until then rag_quality and context_retention cannot be measured at all - they would score ~2/10 as non-answers against a ~9/10 baseline and read as a model regression."
            exit 1
          }

      - name: Run the three eval categories serially
        # Own timeout so an overrun fails HERE, by name, with the collect /
        # integrity / compare / upload steps still running on the partial results.
        # A job-level timeout instead CANCELS, which skips every `!cancelled()`
        # step and discards the categories that did finish. See the budget
        # derivation on the job above. Same convention as email_scorecard_refresh.yml.
        timeout-minutes: 400
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          # Blank when the OAuth token is present, deliberately: runner.py adds
          # `--bare` only when ANTHROPIC_API_KEY is set, and `--bare` restricts
          # auth to the key alone, so leaving both set would ignore the token.
          ANTHROPIC_API_KEY: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN == '' && secrets.ANTHROPIC_API_KEY || '' }}
        run: |
          # Deliberately NOT "Stop": the eval's stderr is piped through `2>&1`
          # below, and under Stop the first stderr line from a native command
          # becomes a terminating NativeCommandError. Every failure here is
          # checked explicitly instead.
          $ErrorActionPreference = "Continue"
          $ProgressPreference = "SilentlyContinue"
          # PYTHONIOENCODING makes the eval emit UTF-8; without this PowerShell
          # decodes it as the OEM codepage and the logs come back as mojibake.
          [Console]::OutputEncoding = [System.Text.Encoding]::UTF8
          New-Item -ItemType Directory -Force -Path eval-out | Out-Null

          function Test-Backend {
            try { Invoke-RestMethod -Uri "$env:BACKEND_URL/api/health" -TimeoutSec 5 | Out-Null; return $true }
            catch { return $false }
          }

          # A leftover backend from a cancelled run still holds :4200 on this
          # persistent runner, and the cleanup below does not fire on job
          # cancellation. Without this check the wait loop can get 200 from that
          # STALE server and evaluate the previous checkout while reporting green.
          if (Test-Backend) {
            Write-Host "::error::$env:BACKEND_URL is already serving - a stale GAIA backend is running on this runner. Kill it (gaia kill) and re-run; otherwise this eval measures the previous checkout, not this one."
            exit 1
          }

          # The GAIA UI backend must live in the SAME shell session as the evals:
          # a backgrounded child does not survive a step boundary, which is why
          # test_agent_behavior_e2e.yml starts its server and runs its harness in
          # one session. Hence backend start + all three categories in one step.
          # Start-Process resolves relative paths against the .NET process CWD,
          # which is not guaranteed to track the PowerShell location. Pin the
          # child's working directory AND give it absolute redirect targets, so
          # the backend and its logs both land in the workspace.
          $outLog = Join-Path $PWD "eval-out\backend.out.log"
          $errLog = Join-Path $PWD "eval-out\backend.err.log"
          # The agent under test runs shell commands, so it must not inherit the
          # judge's credentials; only `claude -p` below needs them.
          $judgeCredentials = @{}
          foreach ($name in @("CLAUDE_CODE_OAUTH_TOKEN", "ANTHROPIC_API_KEY")) {
            $judgeCredentials[$name] = [Environment]::GetEnvironmentVariable($name)
            Remove-Item "Env:\$name" -ErrorAction SilentlyContinue
          }
          $backend = Start-Process -FilePath "python" `
            -ArgumentList "-m","gaia.ui.server","--port","4200","--host","127.0.0.1" `
            -WorkingDirectory $PWD `
            -RedirectStandardOutput $outLog `
            -RedirectStandardError $errLog `
            -NoNewWindow -PassThru
          foreach ($name in $judgeCredentials.Keys) {
            if ($judgeCredentials[$name]) { Set-Item "Env:\$name" $judgeCredentials[$name] }
          }
          if (-not $backend) {
            Write-Host "::error::Could not start gaia.ui.server - the venv install is broken or python is not on PATH."
            exit 1
          }

          try {
            # 120 x 5s sleeps = ~10 min of waiting, more when a probe itself
            # times out. The job timeout is the real backstop.
            Write-Host "Waiting ~10 min for the GAIA UI backend on $env:BACKEND_URL (pid $($backend.Id))..."
            $ready = $false
            foreach ($attempt in 1..120) {
              if ($backend.HasExited) {
                Write-Host "::error::gaia.ui.server exited during startup (exit $($backend.ExitCode)). Backend log follows."
                Get-Content $outLog, $errLog -ErrorAction SilentlyContinue
                exit 1
              }
              if (Test-Backend) { $ready = $true; break }
              Start-Sleep -Seconds 5
            }
            if (-not $ready) {
              Write-Host "::error::GAIA UI backend did not become healthy at $env:BACKEND_URL/api/health in time. Backend log follows."
              Get-Content $outLog, $errLog -ErrorAction SilentlyContinue
              exit 1
            }
            Write-Host "Backend healthy."

            # Per-category wall clock. The job timeout was guessed once and cost a
            # 148-min run (30869194618) that produced one category out of three;
            # these lines are what replaces the guess with a measurement. Each is
            # written the moment its category ends - appended to the step summary,
            # not buffered to the end - so a run that overruns or crashes still
            # leaves behind the timings of whatever DID finish.
            #
            # Not `Out-File -Encoding utf8`: on Windows PowerShell 5.1 that
            # prepends a BOM, and a BOM ahead of the leading `##` stops GitHub
            # rendering it as a heading. AppendAllText with UTF8Encoding($false)
            # has an empty preamble, so no BOM on create or append.
            $summaryEnc = New-Object System.Text.UTF8Encoding($false)
            function Add-Summary($line) {
              if ($env:GITHUB_STEP_SUMMARY) {
                [System.IO.File]::AppendAllText($env:GITHUB_STEP_SUMMARY, "$line`r`n", $summaryEnc)
              }
            }
            $swTotal = [Diagnostics.Stopwatch]::StartNew()
            Add-Summary "## Eval wall clock (sjlab-stx-halo-18)"
            Add-Summary ""
            Add-Summary "| category | minutes |"
            Add-Summary "| --- | ---: |"

            # Serial by construction: a plain foreach in one session, never
            # backgrounded. Two concurrent `gaia eval agent` runs race-evict each
            # other's model out of the single Lemonade slot (CLAUDE.md).
            #
            # `gaia eval agent --category X` exits 0 whatever the scenarios score -
            # it writes the scorecard and returns. So a non-zero exit here is a real
            # harness crash, never a regression; the regression verdict comes from
            # the --compare step below.
            #
            # No --agent-type: the committed baselines were produced against the
            # backend default (their `config` records only backend_url/model/budget),
            # so passing one here would compare against a different agent.
            #
            # Never add web_system or vision here: they read this runner's
            # clipboard, windows and screen into the scorecard.
            $categories = if ($env:EMBEDDER_OK -eq 'false') { @("tool_selection") } else { @("rag_quality", "context_retention", "tool_selection") }
            if ($env:EMBEDDER_OK -eq 'false') { Write-Host "::warning::Embedder unavailable (#3016) - measuring tool_selection only. rag_quality and context_retention answer out of embedder-produced chunks and cannot be measured; the integrity gate below reports them as NOT MEASURED (and fails the job when enforce=true)." }
            foreach ($category in $categories) {
              Write-Host "=================================================================="
              Write-Host "  gaia eval agent --category $category"
              Write-Host "=================================================================="
              $swCat = [Diagnostics.Stopwatch]::StartNew()
              # Reset first: a command that fails to LAUNCH leaves $LASTEXITCODE
              # at its previous value, so an unset/stale 0 would read as success.
              $global:LASTEXITCODE = 0
              # Not Tee-Object: on Windows PowerShell 5.1 it has no -Encoding and
              # writes UTF-16, which the `Output:` parse in the next step then has
              # to guess at. Echo + Out-File keeps the log UTF-8 and greppable.
              gaia eval agent --category $category --backend "$env:BACKEND_URL" --model "$env:EVAL_MODEL" 2>&1 |
                ForEach-Object { Write-Host "$_"; "$_" } |
                Out-File -FilePath "eval-out\$category.log" -Encoding utf8
              # Capture BEFORE anything else can clobber it. .NET calls and
              # Write-Host leave $LASTEXITCODE alone, but reading it first keeps
              # that from being a thing a later edit has to remember.
              $code = $LASTEXITCODE
              $swCat.Stop()

              $mins = [math]::Round($swCat.Elapsed.TotalMinutes, 1)
              Write-Host "[TIMING] ${category}: $mins min (running total $([math]::Round($swTotal.Elapsed.TotalMinutes, 1)) min of the 400-min step budget)"
              Add-Summary "| ``$category`` | $mins |"

              if ($code -ne 0) {
                # Never `exit $LASTEXITCODE` - on a launch failure it can be $null,
                # which PowerShell turns into exit 0 and the step passes GREEN
                # having measured nothing.
                Write-Host "::error::gaia eval agent --category $category crashed with exit $code (a harness failure, not a regression verdict)."
                exit 1
              }
            }

            $swTotal.Stop()
            $totalMins = [math]::Round($swTotal.Elapsed.TotalMinutes, 1)
            Write-Host "[TIMING] all three categories: $totalMins min"
            Add-Summary "| **total** | **$totalMins** |"
          }
          finally {
            if ($backend -and -not $backend.HasExited) {
              Write-Host "Stopping GAIA UI backend (pid $($backend.Id))..."
              Stop-Process -Id $backend.Id -Force -ErrorAction SilentlyContinue
            }
          }

      - name: Collect scorecards from the printed run directories
        # Not always(): on a concurrency cancel or a job-level timeout this would
        # pile misleading "missing scorecard" errors on top of the cancellation.
        # An eval STEP timeout is deliberately not in that bucket - it fails
        # rather than cancels, so this runs and names exactly which categories
        # went unmeasured, which is the diagnostic you want.
        if: ${{ !cancelled() }}
        run: |
          $ErrorActionPreference = "Stop"
          # This step COLLECTS; it does not judge. A category with no scorecard is
          # reported here and lands in the integrity gate below as "no scorecard
          # produced", which is where the enforce policy is applied - once, in one
          # place. Exiting non-zero here as well would re-introduce a second,
          # enforce-blind blocking decision, which is exactly the bug being fixed.
          # A genuine harness crash still fails at the eval step itself.
          $level = if ($env:ENFORCE -eq 'true') { 'error' } else { 'warning' }

          $categories = if ($env:EMBEDDER_OK -eq 'false') { @("tool_selection") } else { @("rag_quality", "context_retention", "tool_selection") }
          foreach ($category in $categories) {
            $log = "eval-out\$category.log"
            if (-not (Test-Path $log)) {
              Write-Host "::${level}::No log for $category - the chain stopped before it ran."
              continue
            }
            # The runner prints an ABSOLUTE `Output: <run-dir>` line
            # (src/gaia/eval/runner.py::_print_summary) - a Windows path with
            # backslashes. Take the last match so a re-run inside one invocation
            # wins. Never glob by mtime.
            $hit = Select-String -Path $log -Encoding UTF8 -Pattern '^Output:\s*(.+?)\s*$' | Select-Object -Last 1
            $runDir = if ($hit) { $hit.Matches[0].Groups[1].Value } else { $null }
            $scorecard = if ($runDir) { Join-Path $runDir "scorecard.json" } else { $null }
            if (-not $runDir -or -not (Test-Path $scorecard)) {
              $shown = if ($runDir) { $runDir } else { "<none>" }
              Write-Host "::${level}::Could not resolve a scorecard for $category (parsed run dir: '$shown'). The eval did not print an 'Output:' line, or it did not write scorecard.json - see eval-out\$category.log."
              continue
            }
            New-Item -ItemType Directory -Force -Path "eval-out\$category" | Out-Null
            Copy-Item $scorecard "eval-out\$category\scorecard.json"
            # The full scorecard stays on the runner for the compare step. The
            # public artifact gets a copy without conversation text; reproduce a
            # regression locally with the same `gaia eval agent --category` run.
            python -m gaia.eval.scorecard "eval-out\$category\scorecard.json" "eval-out\$category\scorecard.public.json"
            if ($LASTEXITCODE -ne 0) { Write-Host "::${level}::${category}: could not write the public scorecard copy, so it will not be uploaded." }
            $summary = Join-Path $runDir "summary.md"
            if (Test-Path $summary) { Copy-Item $summary "eval-out\$category\summary.md" }
            Write-Host "${category}: $runDir"
          }

      - name: Integrity gate — did we actually measure?
        if: ${{ !cancelled() }}
        run: |
          $ErrorActionPreference = "Stop"
          # `--compare` only scores scenarios present on BOTH sides, so a run whose
          # scenarios vanished exits 0 and reads as "no regression" (verified: 6 of
          # 7 dropped -> exit 0). This gate asks the other question - is there a
          # measurement at all - and is the only thing between an empty run and a
          # green check. The logic lives in gaia.eval.integrity_gate so it is unit
          # tested (tests/unit/eval/test_integrity_gate.py) rather than being
          # seventy lines of untestable PowerShell.
          #
          # IT HONORS `enforce`, and that is the fix for the state this gate was in.
          # An earlier revision failed UNCONDITIONALLY on the reasoning that report
          # mode may soften "worse" but never "did not measure". Sound in principle;
          # in practice the embedder cannot load on this runner (#3016, an
          # environment defect no PR can fix), so the gate went red on every PR
          # touching its path filter while the job's own summary printed "report
          # mode ... build stays green". A workflow that advertises non-blocking and
          # blocks anyway is worse than either posture: it blocked 7 PRs whose diffs
          # had nothing to do with the eval, and people stop reading a check that is
          # always red. So `enforce` now decides the EXIT CODE for both incomplete
          # and worse-than-baseline; it never decides VISIBILITY. In report mode
          # every finding is still a ::warning:: annotation and still lands in the
          # job summary, so an incomplete run is impossible to mistake for a
          # complete one - it just does not veto an unrelated PR.
          #
          # `skipped` (SKIPPED_NO_DOCUMENT) counts as unmeasured for a subtle reason
          # documented in the module: those scenarios keep their ids, so the
          # missing-scenario check stays clean, and compare files them under
          # "not a quality signal". All three committed baselines have skipped=0.
          $gateArgs = @(
            "--baseline-dir", $env:BASELINE_DIR,
            "--results-dir", "eval-out"
          )
          if ($env:EMBEDDER_OK -eq 'false') {
            $gateArgs += @("--category", "tool_selection")
            $gateArgs += @("--not-measured", "rag_quality", "--not-measured", "context_retention")
            $gateArgs += @("--not-measured-reason", "the RAG embedder will not load on this runner (#3016), and this category answers out of embedder-produced chunks")
          } else {
            $gateArgs += @("--category", "rag_quality", "--category", "context_retention", "--category", "tool_selection")
          }
          if ($env:ENFORCE -eq 'true') { $gateArgs += "--enforce" }

          # Reset first: a command that fails to LAUNCH leaves $LASTEXITCODE at its
          # previous value, so a stale 0 would read as a complete run.
          $global:LASTEXITCODE = 0
          python -m gaia.eval.integrity_gate @gateArgs
          $code = $LASTEXITCODE
          if ($code -ne 0) {
            Write-Host "::error::Integrity gate failed (exit $code) with enforce=$env:ENFORCE. The backend and Lemonade logs stay on the runner (eval-out\backend.*.log); reproduce locally with the same gaia eval agent command if you cannot reach it."
            exit 1
          }

      - name: Compare each category against its committed Gemma baseline
        if: ${{ !cancelled() }}
        run: |
          $regressed = @()
          $failedCompare = $false

          $categories = if ($env:EMBEDDER_OK -eq 'false') { @("tool_selection") } else { @("rag_quality", "context_retention", "tool_selection") }
          foreach ($category in $categories) {
            $current = "eval-out\$category\scorecard.json"
            $baseline = Join-Path $env:BASELINE_DIR "scorecard_$category.json"
            Write-Host ""
            Write-Host "=================================================================="
            Write-Host "  ${category}: $baseline  ->  $current"
            Write-Host "=================================================================="
            if (-not (Test-Path $current)) {
              # NOT $failedCompare: "we did not measure" is the integrity gate's
              # verdict and it has already rendered it under the enforce policy.
              # Counting it again here would fail the job regardless of enforce.
              Write-Host "::warning::${category}: no scorecard produced - nothing to compare (already reported by the integrity gate)."
              continue
            }
            # `--compare` exits 2 on status/score/time regressions, 0 otherwise
            # (src/gaia/cli.py). It only DIFFS - the eval above already ran.
            # Reset first: a launch failure would otherwise inherit the PREVIOUS
            # category's 2 and be misreported as this category regressing.
            $global:LASTEXITCODE = 0
            gaia eval agent --compare "$baseline" "$current"
            switch ($LASTEXITCODE) {
              0 { Write-Host "${category}: no regression vs baseline." }
              2 {
                Write-Host "::warning::${category}: regression detected vs $baseline (see the diff above)."
                $regressed += $category
              }
              default {
                Write-Host "::error::${category}: compare failed with exit $LASTEXITCODE (not a regression verdict - a harness/IO error)."
                $failedCompare = $true
              }
            }
          }

          Write-Host ""
          Write-Host "=================================================================="
          Write-Host "  SUMMARY - enforce=$env:ENFORCE"
          Write-Host "=================================================================="
          if ($regressed.Count -gt 0) {
            Write-Host "Regressed categories: $($regressed -join ' ')"
          } else {
            Write-Host "Regressed categories: none"
          }
          Write-Host "Baselines: $env:BASELINE_DIR"
          Write-Host "Scorecards uploaded as the 'agent-eval-gemma-consolidation' artifact."
          Write-Host ""
          Write-Host "Code-generation and data-analysis scenarios are EXPECTED to regress on"
          Write-Host "this consolidation (35B MoE -> ~4B dense) and that tradeoff is accepted."
          Write-Host "Read the per-scenario diff above before treating a regression as a bug."
          Write-Host ""
          Write-Host "TIME regressions (>2x per-scenario wall clock) count toward the same"
          Write-Host "verdict but are NOT comparable to this baseline: it was captured against"
          Write-Host "a remote Lemonade over ngrok on different hardware. Treat a time-only"
          Write-Host "regression as a hardware artifact until the baselines are regenerated"
          Write-Host "on this runner."
          Write-Host "=================================================================="

          # A compare that exits with something other than 0 or 2 is the TOOL
          # failing, not a verdict - `enforce` governs what a measurement is
          # allowed to say, never whether the tool ran. Always fatal.
          if ($failedCompare) {
            Write-Host "::error::One or more comparisons could not be performed - 'gaia eval agent --compare' itself failed. This is a harness/IO error, not a regression verdict, so it fails regardless of enforce."
            exit 1
          }
          if ($regressed.Count -gt 0) {
            if ($env:ENFORCE -eq "true") {
              Write-Host "::error::Regressions in: $($regressed -join ' '). enforce=true - failing the build. Fix the prompt/tooling and re-run, or (if the regression is intentional) regenerate the baseline with --save-baseline and call it out in the PR description."
              exit 1
            }
            Write-Host "Report mode (enforce=false): regressions logged as warnings, build stays green."
            Write-Host "Re-dispatch this workflow with enforce=true to make them blocking."
          }

          # THIS LINE IS THE REPORT MODE. GitHub appends
          # `if (Test-Path variable:\LASTEXITCODE) { exit $LASTEXITCODE }` to every
          # PowerShell step, so falling off the end here would re-export the LAST
          # `--compare` exit code: a report-mode regression in the final category
          # would fail the build anyway, and whether it did would depend on which
          # category regressed. Same guard as install-lemonade/action.yml.
          $global:LASTEXITCODE = 0

      # The artifact is public and, unlike the job log, not secret-masked. The
      # file list here must match the upload step's `path:` below.
      - name: Refuse to upload a credential
        if: always()
        env:
          CLAUDE_CODE_OAUTH_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          HF_TOKEN: ${{ secrets.HUGGINGFACE_ACCESS_TOKEN }}
        run: |
          $ErrorActionPreference = "Stop"
          $files = @(Get-ChildItem eval-out -Recurse -File -Include scorecard.public.json, summary.md -ErrorAction SilentlyContinue)
          $leaked = @()
          foreach ($name in @("CLAUDE_CODE_OAUTH_TOKEN", "ANTHROPIC_API_KEY", "HF_TOKEN")) {
            $value = [Environment]::GetEnvironmentVariable($name)
            if (-not $value) { continue }
            foreach ($file in $files) {
              if ((Test-Path -LiteralPath $file.FullName) -and (Select-String -LiteralPath $file.FullName -SimpleMatch -Pattern $value -Quiet)) {
                $leaked += $file.FullName
                Remove-Item -LiteralPath $file.FullName -Force
              }
            }
          }
          if ($leaked.Count -gt 0) {
            Write-Host "::error::A credential appeared in $($leaked.Count) file(s) bound for the public artifact. They were deleted, not uploaded: $($leaked -join ', ')"
            exit 1
          }
          if ($files.Count -eq 0) {
            Write-Host "::warning::No scorecard.public.json or summary.md under eval-out - nothing was scanned, and nothing will upload."
          } else {
            Write-Host "Scanned $($files.Count) file(s): no credential found."
          }

      # Must match the scan step's -Include list above.
      - name: Upload scorecards
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: agent-eval-gemma-consolidation
          path: |
            eval-out/*/scorecard.public.json
            eval-out/*/summary.md
          if-no-files-found: warn
          retention-days: 14
